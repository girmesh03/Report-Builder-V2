# Implementation Review Log — Phase Verdicts, Findings, And Required Fixes

> This log is the **review record** of the implementation phases of Report Builder V2. It is written and maintained **exclusively by the review AI** (invoked with `use docs/review-implementation.md <N>`): after exhaustive analysis of every codebase change and every detail of the implementation against the specification and logical reasoning, the review AI records the phase verdict and every finding that the implementation AI must address. The **implementation AI has read-only access** — it reads this log in the fix loop (`use docs/implementation-review-log.md <N>`), applies the findings, and never writes here. **Every finding is binding until confirmed fixed. When a fix is confirmed, the review AI updates the previously stated finding in place** (marks it `RESOLVED` and re-states the confirmed outcome); no finding is left stale. A phase is merged only after its verdict is GREEN and the user approves.

## Status Legend

| Status | Meaning |
|---|---|
| `PENDING` | Phase implemented but not yet reviewed. |
| `GREEN` | Review passed — the phase is per the specification and logically sound; nothing outstanding; the phase can be merged after user approval. |
| `FAIL` | Review found issues — the findings below are binding for the implementation AI; the phase re-enters the fix loop until a re-review logs GREEN. |

Per-finding status (updated in place by the review AI):

| Finding status | Meaning |
|---|---|
| `OPEN` | Raised by the review AI; not yet fixed and confirmed. |
| `RESOLVED` | The implementation AI fixed it and the review AI re-verified it; the finding statement is updated in place to the confirmed outcome. |

## How To Use This Log

- **Review AI** — after the user says `use docs/review-implementation.md <N>` (plan mode) and then `proceed` (build mode): review the phase exhaustively, then record the phase entry below — verdict and findings only. The findings state **exactly what the implementation AI must do**. Update the status table.
- **Review AI update rule** — when the implementation AI returns a fixed phase and the fix is confirmed by re-verification, update the previously stated findings in place: change each confirmed finding to `RESOLVED` with the confirmed outcome, and flip the phase verdict to `GREEN` when nothing remains open. Never delete a finding; never leave an outdated statement.
- **Implementation AI** — when the user says `use docs/implementation-review-log.md <N>`: read the phase entry (read-only), return to Step 2 (Deep Codebase Analysis) of `## Phase Protocol`, apply **every** `OPEN` finding, record the corrections in `docs/implementation-log.md` (Step 6 action 1), and re-present the phase for re-review. Never write to this log.
- The log records review **content**, not review process: verdicts, findings, and required fixes — not branches, commits, dates, or the commands the review AI ran.

## Status

| Phase | Verdict |
|---|---|
| 1 | GREEN |
| 2 | GREEN |
| 3 | GREEN |
| 4 | GREEN |
| 5 | PENDING |
| 6 | PENDING |
| 7 | PENDING |
| 8 | PENDING |

## Log

### Phase 1 — Foundation

- **Verdict:** GREEN
- **Findings:** Re-review of the fix rounds (rounds 2–3): every round-1 finding is confirmed fixed and marked `RESOLVED` in place below. Round 3 re-verified the final one — the invented password `min: 6` rule is removed — so nothing remains open and the phase is per the specification and logically sound; it can be merged after user approval (commit remains under user control).
- **Specification deviations:**
  - `RESOLVED` — **DM-03 applied to all five models.** Confirmed by re-verification: `backend/models/user.model.js`, `branch.model.js`, `report.model.js`, `audio.model.js`, and `transcription.model.js` all now set `timestamps: true` and carry the DM-03 `toJSON`/`toObject` transforms deleting `id` and `__v`; the User additionally deletes `password` and keeps `virtuals: true` (so the `fullName` virtual survives). The `report.model.js` `schema.index({ user: 1, createdAt: -1 })` is now valid because `createdAt` is populated by the timestamps option. Fix confirmed; nothing further required.
  - `RESOLVED` — **Validators renamed to the `<domain>.validator.js` pattern.** Confirmed by re-verification: `backend/validators/` now contains `auth.validator.js`, `branch.validator.js`, `report.validator.js`, `audio.validator.js`, `transcription.validator.js`, `ai.validator.js`, `user.validator.js`, `analytics.validator.js` (REQ-176/§4 tree naming), route imports updated (zero stale references to the old names), and `validators/validation.js` is recorded in the `## Project Directory Structure` §4 tree (REQ-173). Fix confirmed; nothing further required.
  - `RESOLVED` — **REQ-196 error logging fixed.** Confirmed by re-verification: `backend/utils/logger.js` printf now emits `status=<statusCode>` and appends the stack trace, and `backend/middleware/error.middleware.js` passes `{ stack, statusCode }`. Runtime evidence in the `backend/logs/` daily file: error entries now render as `ERROR status=500 Unexpected error` followed by the full stack (previously bare lines). The development/production split is intact (dev: error message + stack in the response; production: generic message, internals in logs only). Fix confirmed; nothing further required.
  - `RESOLVED` — **DM-04 applied to Transcription.** Confirmed by re-verification: `backend/models/transcription.model.js` loads `mongoose-paginate-v2` and registers `transcriptionSchema.plugin(mongoosePaginate)`, so the Transcription list endpoints (T-3-04b) can paginate. Fix confirmed; nothing further required.
- **Invalid business logic implemented:** none.
- **Edge cases to be handled:**
  - `RESOLVED` — **Client production build verified by the fix loop.** Confirmed by re-verification: the fix round recorded a 0-error `npx vite build` (MUI chunk-size warning only) with `client/dist/` deleted afterward — `client/dist` is absent from the working tree — and the client lint pass (exit 0) still holds. Fix confirmed; nothing further required.
- **Conflicts to be resolved:**
  - `RESOLVED` — **ChatConversation model count (six vs five).** `## Data Modeling` §1 inventories six persisted models including ChatConversation, while the §4 tree and T-1-08 list five, and Phase 1 implemented five (User, Branch, Report, Audio, Transcription). User decision: Phase 1 stays at five models. Required for the implementation AI: create `backend/models/chatConversation.model.js` **in Phase 5** (per T-5-02b and AD-010, which place ChatConversation record creation in Phase 5) and update the §4 tree at that point (REQ-173) — do not create the model in Phases 2–4. Decision confirmed; the Phase 5 action remains pending by design.
  - `RESOLVED` — **Invented password `min: 6` rule removed.** Confirmed by round-3 re-verification: `backend/validators/auth.validator.js` `validateRegister` no longer contains `body('password').isLength({ min: 6 })`; the password now carries only the `notEmpty()` presence check matching `validateLogin`, so no invented constraint and no hardcoded validation constant remain (REQ-083). The correction is recorded in the Phase 1 entry of `docs/implementation-log.md` (review round 2). The final register validation rules are defined in Phase 2 per T-2-01b, with constants living in `backend/utils/constants.js`. Fix confirmed; nothing further required.
  - `RESOLVED` — **Spec typo: T-1-04d cited REQ-085 for the console.log ban; the ban is REQ-086 and applies to the backend only** (frontend `console.log` is permitted if needed — user decision). Confirmed by re-verification: the Step 6 alignment pass corrected T-1-04d to cite REQ-086 in `docs/specification.md`, and the backend remains `console.log`-free (grep-verified). Fix confirmed; nothing further required.
  - `RESOLVED` — **Step 6 record performed; commit deferred.** Confirmed by re-verification: the Phase 1 entry of `docs/implementation-log.md` is now filled (Implemented, Changes/updates/corrections, Validation results) per Step 6 action 1. The commit/push remains under user control (commit only on explicit user request). Nothing further required.
- **Other required actions:**
  - `RESOLVED` — **`makeQueryWritable` documented.** Confirmed by re-verification: the Express 5 `req.query` getter-only-accessor shim and its position between cookie-parser and mongo-sanitize are now recorded in `docs/specification.md` — in `## Backend Architecture` §5 and `## Security` §5 — with the rationale (express-mongo-sanitize 2.2.0 assigns to `req.query` and would throw). The mandated six-step order (REQ-081) is preserved. Fix confirmed; nothing further required.
  - `RESOLVED` — **Required-field message convention (clarification).** Review question: must model schemas use `required: [true, 'message']`? Answer (evidence-based): no — the canonical model form (`## JSDoc Standards` §11, the `dailyReport.js` sample) and every `## Data Modeling` §4 field table declare required fields as plain `required: true`; the modeling rule set (§3, DM-01..DM-07) contains no message requirement. Custom validation messages are the contract of the express-validator layer (`withMessage`, already used in the validators) and the RHF form rules (`required: 'Email is required'`). The implemented models already match the canonical form exactly — no model change required; the GREEN verdict is unaffected. Required for the implementation AI (deferred by design to **Phase 2 Step 6**): add a modeling rule to `## Data Modeling` §3 (e.g. **DM-08**): "Required model fields use plain `required: true`; custom validation messages are defined in the express-validator (`withMessage`) and RHF layers — never in model schemas." — making the convention explicit and binding for Phases 2–8, which add many new required fields.
  - Observation (no action) — the full `authenticate` middleware exceeds T-1-05a's "stub" wording, but it is justified by S-1-05a (the protected route must return 401 before the token contract exists).
  - Observation (no action) — the `/validate` probe endpoints and the `/protected` route are temporary scaffolding; they are replaced by the real contracts in Phase 2.
  - Observation (no action) — `client/src/components/layout/AppErrorBoundary.jsx` is a hand-rolled class component while `react-error-boundary` (installed, unused) is referenced by `## Error Handling` §3; the implementation meets REQ-202's essence, and a switch to the package's `ErrorBoundary` may be considered in a hardening phase — not blocking.

### Phase 2 — Authentication And User Management

- **Verdict:** GREEN
- **Findings:** Re-review of the fix round (round 4): all four round-3 items are confirmed fixed and marked `RESOLVED` in place below — **F-2-08** and **F-2-09** restored the spec §8 verbatim copy in `RegisterForm.jsx`, and **C-2-01**/**C-2-02** wrote the user decisions into the spec (REQ-095 + §12.4 + §6, and REQ-099 + §12.3 + §14 + §7). The round-1 and round-2 findings remain `RESOLVED`. Nothing remains open — the phase is per the specification and logically sound and can be merged after user approval (the commit remains under user control; none was made per user instruction). Gates re-run on the round-4 state: `node --check` on every backend file (PASS), client `npm run lint` (exit 0), `python scripts/verify-initial-doc.py` (exit 0, SELF-ALIGNED), zero `from '@mui/material'` barrel imports in `client/src`, zero remnants of the old register copy or the old confirmPassword message in `client/src`, `client/dist` absent.
- **Specification deviations:** (each finding: what is wrong / the violating spec section and evidence / exactly what the implementation AI must do — status `OPEN`)
  - `RESOLVED` — **Route objects use `element:` instead of `Component:` on the pathless wrapper routes (REQ-094, §12.5 route tree).** Confirmed by re-verification: `client/src/main.jsx` now declares `Component: PublicRoute`, `Component: PublicLayout`, `Component: ProtectedRoute`, and `Component: AppShell` on the four pathless routes (lines 45, 48, 58, 61); a grep of `client/src` finds zero `element:` usages in the route tree; the tree still renders identically via `<Outlet />` under React Router 8.2.0. Fix confirmed; nothing further required.
  - `RESOLVED` — **`ErrorBoundary: AppErrorPage` never wired into the router (spec §12.5 route tree, line 335).** Confirmed by re-verification: `client/src/main.jsx` line 42 now adds `ErrorBoundary: AppErrorPage` to the root route object, so `AppErrorPage.jsx` is live as the router error fallback instead of dead code. Fix confirmed; nothing further required.
  - `RESOLVED` — **`*` NotFound is a sibling of AppShell instead of a catch-all inside AppShell children (spec §12.5 route tree, line 335; §12.6 NotFound, line 3194).** Confirmed by re-verification: `client/src/main.jsx` line 69 now places `{ path: '*', Component: NotFound }` inside AppShell's children (404 renders with the shell chrome); `assistant` (line 72) remains the only protected route outside AppShell. Fix confirmed; nothing further required.
  - `RESOLVED` — **`@mui/material` barrel imports of `useMediaQuery` (REQ-107, §14.1: "never import from the `@mui/material` barrel").** Confirmed by re-verification: all five files now import the deep path default export — `import useMediaQuery from '@mui/material/useMediaQuery'` in `MuiDialog.jsx`, `MuiAppbar.jsx`, `GlobalSearchDialog.jsx`, `AppSidebar.jsx`, `AppShell.jsx` — and `useTheme` is back on `@mui/material/styles` in AppSidebar/AppShell (AppSidebar also takes `alpha` from styles); a grep of `client/src` confirms zero `from '@mui/material'` barrel imports remain; the implementation-log note claiming the barrel was forced is corrected. Fix confirmed; nothing further required.
  - `RESOLVED` — **GlobalSearchDialog uses raw `@mui/material/Dialog` instead of MuiDialog (REQ-109, §14.3: MuiDialog "is always used instead of raw `@mui/material/Dialog`").** Confirmed by re-verification: `client/src/components/reusable/GlobalSearchDialog.jsx` now composes `MuiDialog` (lines 19, 69–78) with no raw Dialog import; `fullScreen`, `onClose`, and `PaperProps` pass through (the explicit 600px / 720px widths, 80vh / 70vh heights, and the <600px / <768px-landscape fullscreen rules of §12.3 are preserved); the header row and the grouped-accordion results remain as children in the scrollable content. Fix confirmed; nothing further required.
  - `RESOLVED` — **Landing page is not implemented — the Phase 1 shell is live UI (spec §12.6 §6, lines 3144–3149; user decision 2026-08-03: "if it's planned on future phases then at least the basic landing page must be implemented on this phase").** Confirmed by re-verification: `client/src/pages/Landing.jsx` now renders the §6 hero — centered wrapper `max-width: 1200px` (`mx: 'auto'`, `textAlign: 'center'`), `Description` logo/icon, headline "Build Better Reports" (`typography: { xs: 'h4', md: 'h3' }` per §6's h4-on-xs / h3-on-md+ rule), subheadline "Record, transcribe, and generate professional reports with AI" (verbatim), CTAs "Get Started" (contained → `/register`) and "Sign In" (outlined → `/login`) via `MuiButton` + `useNavigate()`; static page (no data fetching, no Redux); both text elements `noWrap` (ellipsis per §12.4); no horizontal scroll; `displayName` set. Fix confirmed; nothing further required.
  - `RESOLVED` — **`textAlign` passed as a DOM prop on Typography triggers the React warning "React does not recognize the `textAlign` prop on a DOM element" (REQ-107, §14.1 styling: `sx` only, no DOM styling props; user decision 2026-08-03: deprecated/MUI-rule violations must be respected).** Confirmed by re-verification: `client/src/components/login/LoginForm.jsx` and `client/src/components/register/RegisterForm.jsx` now render the nav-link Typography as `sx={{ mt: 2, textAlign: 'center' }}` with no `textAlign` prop — the React DOM warning is gone; a JSX-wide grep confirms the only remaining `textAlign=` usages are the legitimate MUI v9 Divider labeled-divider API at `LoginForm.jsx:91` and `RegisterForm.jsx:95`. Fix confirmed; nothing further required.
  - `RESOLVED` — **Register form header copy deviates from the verbatim spec §8 text (spec §8, line 3166: "Same card layout as Login; title 'Sign Up'; subtitle 'Create your account to get started'").** `client/src/components/register/RegisterForm.jsx` lines 78–82 rendered the title "Create Account" and the subtitle "Sign up to start building reports" instead of the spec-pinned 'Sign Up' / 'Create your account to get started'. Confirmed by re-verification: `RegisterForm.jsx` now renders the spec §8 header verbatim — title "Sign Up" (line 79), subtitle "Create your account to get started" (line 82) — matching spec §8 line 3167 exactly; the user confirmed restoring the verbatim copy (recorded in the Phase 2 entry of `docs/implementation-log.md`), which also removes the header-vs-submit-button inconsistency; a `client/src` grep finds zero remnants of the old copy. Fix confirmed; nothing further required.
  - `RESOLVED` — **confirmPassword validation message deviates from the verbatim spec §8 text (spec §8, line 3168: `validate: (v) => v === getValues('password') || 'Passwords must match'`).** `client/src/components/register/RegisterForm.jsx` lines 150–151 used `validate: (value, values) => value === values.password || "Passwords do not match"`. Confirmed by re-verification: `RegisterForm.jsx` lines 150–151 now use `value === values.password || "Passwords must match"` — the exact message pinned by spec §8 line 3169, REQ-114 (line 1402), and §15 rule 5 (line 379); the RHF two-argument validate form is retained (review confirmed functional equivalence). Fix confirmed; nothing further required.
- **Invalid business logic implemented:** none.
- **Edge cases to be handled:** none.
- **Conflicts to be resolved:**
  - `RESOLVED` — **DM-08 modeling rule required by the Phase 1 review was not added in Phase 2 Step 6.** Confirmed by re-verification: `docs/specification.md` `## Data Modeling` §3 now carries the DM-08 row (line 1973) — "Required model fields use plain `required: true`; custom validation messages are defined in the express-validator (`withMessage`) and RHF layers — never in model schemas." — exactly as required by the Phase 1 entry; the correction is recorded in the Phase 2 entry of `docs/implementation-log.md`. Fix confirmed; nothing further required.
  - `RESOLVED` — **PublicRoute Landing exception vs REQ-095 / §12.4 / §6 (user decision 2026-08-03, recorded in `docs/implementation-log.md`).** The user decided authenticated users may view `/` (Landing) but not `/login`, `/register`, or any other public page; `client/src/components/routes/PublicRoute.jsx` now implements `if (isAuthenticated && location.pathname !== '/')` → redirect, and the sidebar/app-bar logo navigates to `/` accordingly. This contradicted the spec texts — REQ-095's guard acceptance ("PublicRoute redirects authenticated users to `/dashboard`"), §12.4's guard rule, and §6 Landing's "PublicRoute redirects authenticated users away" — which were stale. Confirmed by re-verification: the spec now states the Landing exception in all four places the finding required — the §12.4 table row, REQ-095 (requirement + acceptance criteria), the §6 Landing bullet ("PublicRoute lets authenticated users view the Landing page; `/login`, `/register`, and any future public pages still redirect to `/dashboard`"), and the §12.4 `PublicRoute` detail ("except on `/` (Landing), which authenticated users may view") — and `PublicRoute.jsx`'s pathname-based guard matches the aligned text. Fix confirmed; nothing further required.
  - `RESOLVED` — **GlobalSearchDialog and auth-page logo sizes vs REQ-099 §12.3 and spec §7 (user decisions 2026-08-03, recorded in `docs/implementation-log.md`).** The user settled the GlobalSearchDialog at width 600px in both tiers, height 80vh in both tiers, and the large-viewport cutoff at `up('md')` — `GlobalSearchDialog.jsx` (`DIALOG_WIDTH_SMALL` 600 / `DIALOG_WIDTH_LARGE` 600, `dialogHeight: '80vh'`, `isLargeViewport = useMediaQuery(theme.breakpoints.up('md'))`) — and the login/register/landing card logos render at 24px (`fontSize="large"` on `Description` / the Google icon) with no fixed-pixel icon sizes anywhere. These contradicted the spec texts — REQ-099 §12.3's numbers (80vh/600px for 600–1200px; 70vh/720px above 1200px) and §7's `fontSize: 48px` logo rule — which were stale. Confirmed by re-verification: the spec now carries the settled values — the §12.3 table row, REQ-099, and the §14 GlobalSearchDialog component detail all read "centered 600px wide / 80vh tall on all larger screens (≥600px)" (full-screen below 600px / 768px landscape unchanged), and spec §7's Login logo rule now reads `fontSize="large"` + `color="primary"` — MUI props only, no fixed-pixel icon sizes per user decision — matching `GlobalSearchDialog.jsx` (600/600/80vh, `up('md')`) and the logo usages in `Landing.jsx`/`LoginForm.jsx`/`RegisterForm.jsx`. Fix confirmed; nothing further required.
- **Other required actions:**
  - Answered verification question (no action) — **"Do all `throw`s in the controllers reach the global error handler?" Yes, verified.** Every controller handler is wrapped in `express-async-handler` (REQ-199, §10.3, §28.1: "rejected promises forward automatically to the global error handler through `next(error)`"), and `asyncHandler` catches synchronous throws and rejected promises and forwards them via `next(error)`. Confirmed in code: all seven handlers in `backend/controllers/auth.controller.js` (`register`, `login`, `logout`, `refresh`, `me`, `googleOAuthStart`, `googleOAuthCallback`) and both in `backend/controllers/user.controller.js` are `asyncHandler`-wrapped, so every `throw new CustomError(...)` (400/401/409/503) reaches the global `error.middleware.js`. There are no controller catch blocks doing `next(error)` because none are needed; the only internal catch (`googleOAuthCallback`'s OAuth exchange) is deliberate flow control (log + redirect to `/login`), consistent with the stubbed flow. The `next(error)`-in-catch pattern exists only in the unwrapped middlewares (`authenticate.middleware.js:27,32,37`, `notFound.middleware.js:16`), which implement it correctly. No unused `next` parameter exists anywhere in the backend — the one historical case (the pre-save hook) was removed in the e2e fix round, and the unused `_next` in `error.middleware.js:31` follows the `_`-prefix convention.
  - Verified clean in round 3 (record for the record): the additional user-reported fixes recorded in the Phase 2 `docs/implementation-log.md` changes were re-verified against the spec — `authSlice.js` endpoint renamed `me` → `getMe` (RTK Query derives `useGetMeQuery` from the endpoint name; the URL stays `GET /api/v1/auth/me`; matchers and exports consistent); `LoadingSpinner.jsx` moved all system props into `sx` (MUI 9.2.0 no longer consumes them on `Box`); `GlobalSearchDialog.jsx` uses `slotProps.paper` instead of the removed `PaperProps` and adds the Close clear button via `useWatch` (React Compiler-compatible) while preserving the back-arrow reset; `MuiAppbar.jsx` public left section is a single clickable logo unit (icon + app name from `API_CONFIG.VITE_APP_NAME`, name hidden on xs) with the protected variant logo-less per user decision; `AppSidebar.jsx` nav items `flex: '0 0 auto'` and mini-mode `ListItemIcon minWidth: 16`, logo clickable → `/` under the new guard; `PublicLayout.jsx` `marginTop: { xs: 7, sm: 8 }`; `MuiDialog.jsx` adds the user-requested `contentSx` prop (observation below). A JSX-wide grep confirms every remaining styled prop is a legitimate MUI component prop (`fontSize`/`color` on icons and Typography/Button, `Divider textAlign`) — zero leaked system props.
  - Observation (no action) — `MuiDialog.jsx` gains the custom `contentSx` prop (REQ-108 §14.2: wrappers carry "no custom API surface"). User-requested and recorded in `docs/implementation-log.md`; there is no standard MUI pass-through for the wrapper's internal `DialogContent` (Dialog has no content slot), so a custom prop is the only way to control content padding. Accepted.
  - Observation (no action) — REQ-097 says the temporary drawer "opens via the header menu icon"; the implementation opens it via AppShell's floating menu icon and the AppSidebar header icon closes it on mobile. Functionally equivalent (the header icon is unreachable while the drawer is closed) and recorded as a Phase 2 decision in `docs/implementation-log.md`; accepted.
  - Observation (no action) — the Login/Register outer container implements `minHeight: '100%'` inside a `100vh` flex root where spec §7 states `minHeight: calc(100vh - 64px)`; both resolve to the same visual result within PublicLayout. Accepted.
  - Verified clean (record for the record): all 39 backend files pass `node --check`; client `npm run lint` exit 0; `python scripts/verify-initial-doc.py` exit 0 (SELF-ALIGNED); no `console.*` in the backend; `process.env` confined to `config/env.js`; `.env` first line of `.gitignore`, `backend/logs/` and `backend/uploads/audio/` ignored; backend boot + live e2e evidence in `backend/logs/backend-2026-08-03.log` (server on 4000, DB connected) and the Phase 2 validation record; backend auth contract (REQ-090 name extraction, REQ-089 bcrypt compare, REQ-087 rotation/replay rejection, cookie pair 15m path `/` + 7d path `/api/v1`, httpOnly/lax/secure-in-prod), rate tiers (global 100/15min in `app.js`, auth 20/15min on register/login), middleware order, OAuth stub (503, scopes incl. `drive.file` per REQ-158/AD-012, callback match-or-create by email), `baseQueryWithReauth` refresh→retry→logout with `AUTH_NO_REFRESH_PATHS`, guards per REQ-095, shell per REQ-096, sidebar per REQ-097, GlobalSearchDialog per REQ-099, and the Login/Register copy and data flows per spec §7–§8 all verified exact against the spec.

### Phase 3 — Domain Models And Core Reporting

- **Verdict:** GREEN
- **Findings:** Fresh full-cycle re-review of the final Phase 3 state — verdict **GREEN** reconfirmed, nothing new remains open; the `RESOLVED` statements below (F-3-01, F-3-02, F-3-03, F-3-04, C-3-01, live smoke) stand as written, and **F-3-05** stands as the binding Phase 8 alignment note. This cycle explicitly re-verified, first-hand, the two user-reported corrections that the earlier rounds had covered only implicitly: (1) **`req.validated` nested-per-location shape** — `backend/validators/validation.js:33–37` builds `{ body: matchedData(req, { locations: ['body'] }), params: …, query: … }` per spec §10.10 (the flat `matchedData` form the express-validator 7.3.2 `lib/matched-data.js` `_.set` produces is gone), and all eleven controller access sites read `req.validated.body` — auth 139/161, user 29, branch 59/79, report 109/140, transcription 24/98, audio 25 — so the user-reported 500-on-every-create defect class is eliminated; (2) **`_id` identity convention** — `MuiDataGrid.jsx:45` keys rows via `getRowId` defaulting to `(row) => row._id` (overridable), and a `client/src` grep finds zero `id` reads from API docs; the columns (`row._id` for View/Edit/Delete), `BranchSelectorDialog` (keys/toggles/checkedIds/`branchId` payload), `ReportFilterDialog` (select values), `BranchFormDialog` (`updateBranch({ id: branch._id })`), `GlobalSearchDialog` (navigation + keys), and `ReportDetails` (`branchId?._id ?? branchId`, `clip._id`) all use `_id` — consistent with DM-03 stripping the `id` virtual (Phase 1). The `docs/specification.md` additions this phase are the user-approved correction records (client identity convention in `## Redux RTK Query` §2 and §9.6 `getRowId`; MuiEmptyState §9.10; MuiDialog `contentSx` retirement and the 400px cap in §14/§5; v9 toolbar reconciliation and `csvOptions.getRowsToExport` in §9.6; the `<Suspense>` spinner in §1.2; the selection-list `{ skip: !open }` lazy fetch in §2) — each matches the implemented code. The Ethiopian date math (F-3-04 leap rule `year % 4 === 3`, C-3-01 DD-MM-YY display with 2-/4-digit typing) remains as re-verified numerically in the second fix round, including the leap-rule correction: the Ethiopian calendar's leap years are `% 4 === 3` (2015, 2019, 2023 leap with a 6-day Pagume; 2016, 2018, 2020 common), cross-checked against Wikipedia's Ethiopian-calendar article (6th epagomenal day on 29 August of the Julian calendar, six months before the corresponding Julian leap day) and the converters time.et / ertale, and internally consistent with the code's own 1461-day block math (`r = 1460` ⇒ year `4q + 3` in `jdnToEthiopian`); the review's originally prescribed `% 4 === 0` was wrong and the implementation AI's deviation is accepted as the fix. Gates re-run independently on the final state: `node --check` on every backend file (PASS), client `npm run lint` (exit 0), `python scripts/verify-initial-doc.py` (exit 0, SELF-ALIGNED), `client/dist` absent; the fix rounds' 0-error `npx vite build` is accepted as evidence (re-running it would recreate `client/dist` — standing guideline bans build side effects). The binding live backend smoke was executed and recorded in fix round 1 (see "Other required actions"); the Phase 4 business-logic integration analysis below remains in force for Phase 4.
- **Specification deviations:** (each finding: what is wrong / the violating spec section and evidence / exactly what the implementation AI must do — status `OPEN`)
  - `RESOLVED` — **F-3-01 (BLOCKING): `Audio.paginate` does not exist — `GET /api/v1/audio` throws on every call.** Confirmed by re-verification: `backend/models/audio.model.js` now imports `mongoose-paginate-v2` (line 6) and registers `audioSchema.plugin(mongoosePaginate)` (line 51) immediately before `mongoose.model('Audio', …)`, mirroring the `report.model.js` pattern exactly; the file's JSDoc `@typedef`/`@property` header is intact. The binding live smoke was executed and recorded in the Phase 3 implementation-log entry: `GET /api/v1/audio` returns 200 with `data.docs` and `data.totalDocs` (throwaway user, probe data deleted, port released). `node --check` passes on the model. Fix confirmed; nothing further required.
  - `RESOLVED` — **F-3-02 (HIGH): ReportFilterDialog drafts never reflect the active filters — reopening the dialog shows mount-time defaults and Apply silently wipes active filters.** Confirmed by re-verification: `client/src/components/report/ReportFilterDialog.jsx` now re-syncs `draftDate`/`draftBranchId`/`draftArchived` from the `filters` prop on every false→true `open` transition via the render-time `prevOpen` snapshot (lines 61–67 — setState-during-render, the same pattern as `BranchSelectorDialog.jsx:49–53`), so every reopen mirrors the active filters and Apply can no longer drop them invisibly; Cancel still resets to defaults. Mechanism note: the first review prescribed an `open`-keyed `useEffect`, but the project's React Compiler lint (`eslint-plugin-react-hooks` 7.1.1 flat recommended — verified in `client/eslint.config.js`) flags direct setState-in-effect, and the lint exit-0 gate confirms the adopted pattern passes; the sibling `BranchFormDialog.jsx:44–48` effect survives because RHF's `reset` is not flagged as setState — functionally equivalent. The pinned §10 details are preserved (centered `maxWidth="sm"`, `fullScreen={false}`, paper `m:1 py:1`, `p:0` content + 400px scroll cap, MuiDatePicker ClearIcon end adornment, branch-clear IconButton, "Archived" switch `labelPlacement="start"`, `skip: !open` branch query). Fix confirmed; nothing further required.
  - `RESOLVED` — **F-3-03 (MEDIUM): MuiDatePicker's dayjs construction rolls over for Ethiopian month 2 (Tikimt) days 29–30 — the input shows the wrong DD-MM-YY on ~2 days per year.** Confirmed by re-verification: `client/src/components/reusable/MuiDatePicker.jsx` was reworked in the fix round (two attempts — the first custom field crashed at mount because MUI X's `useField` machinery requires its textField slot to render a `PickersSectionList`, source-verified; the final version is the self-contained `EthiopianDateField` that bypasses that machinery entirely). The field renders `formatEthiopianDateLong(gregorianToEthiopian(value.year(), value.month() + 1, value.date()))` (lines 90–96) — the display is derived directly from the Gregorian anchor via the conversion utilities, the dayjs `format` string is never used, so no proxy date can ever roll over; typing parses Ethiopian text via `parseEthiopianDate` → `ethiopianToGregorian` → `dayjs(new Date(...))` committed through `context.setValue` with `source: "field"` and `changeImportance: "set"` (lines 98–114); `usePickerContext` is confirmed a public export of the installed `@mui/x-date-pickers/hooks` (v9.9.0); the Gregorian-anchor `value`/`onChange` contract is unchanged for the consumers (`Reports.jsx`, `CreateReportDialog.jsx`); the trailing CalendarIcon open button, the `triggerStatus` handling, and the user's `slotProps.input.endAdornment` (ReportFilterDialog's ClearIcon) are all preserved (lines 137–158, 179–182). Correction to the first review's own sample pairs (re-derived numerically with the actual utility functions — the review's pairs were off by one day/year): Tikimt 29 = Gregorian 2025-11-08, Tikimt 30 = Gregorian 2025-11-09, Pagume 1–5 of Ethiopian year 2018 = Gregorian 2026-09-06 … 2026-09-10, and Gregorian 2026-09-11 = Ethiopian 2019-01-01 (New Year); the earlier statement "Eth 30-02-18 = Greg 08-11-2025" and the "Pagume 1–6 → 06/11-09-2025" numbers are superseded by these verified values. The rollover defect itself was real and is gone. The display-format consequence of this fix (DD-MM-YYYY vs the spec-pinned DD-MM-YY) is tracked as conflict C-3-01 below. Fix confirmed; the C-3-01 display item remains binding.
- **Invalid business logic implemented:** none beyond F-3-01 (logged above). Verified line by line as per spec: the status machine (draft → audio_attached via createAudio with the 409-on-completed guard at `audio.controller.js:31`; audio_attached → transcribed via createTranscription with the 409s at `transcription.controller.js:30,33`; updateTranscription's transcribed-state 409 at line 96); the `generated → generatedHistory[]` replacement logic (previous text pushed as `{ provider, text }`, provider 400-required when replacing, AD-010); the user-scoped, paginated list queries with the `date`/`branchId`/`isArchived`/`search` filters (`report.controller.js:26–36,64,111,146`); the `{ success, message, data }` envelope; the 422 validation shape with `req.validated = { body, params, query }`; and the `Report.branches[].branchId` populate path with `REPORT_POPULATE_PATHS` (branch name/location, audio metadata, transcription raw + latest + history).
- **Edge cases to be handled:**
  - `RESOLVED` — **F-3-04: the Pagume day cap is now leap-year-aware.** The first review's prescribed `day > 6` cap was applied verbatim in fix round 1 (`client/src/utils/ethiopianDate.js`), but the review's leap-year rule was wrong: Ethiopian leap years are `year % 4 === 3`, not `year % 4 === 0` (2015, 2019, 2023 leap — Pagume has 6 days; 2016, 2018, 2020 common — 5 days). Fix round 2 implemented the corrected guard `month === 13 && day > (year % 4 === 3 ? 6 : 5)`. Verified first-hand against the installed utilities: `ethiopianToGregorian(2015, 13, 6)` → 2023-09-11 round-trips to `{2015, 13, 6}` (leap); `(2019, 13, 6)` → 2027-09-11 round-trips (leap); `(2016, 13, 6)` → 2024-09-11 round-trips to `{2017, 1, 1}` (common — the day does not exist, so the parser must reject it); `parseEthiopianDate('06-13-2016')` → null, `parseEthiopianDate('06-13-2019')` → `{day: 6, month: 13, year: 2019}`, `parseEthiopianDate('06-13-2018')` → null, `parseEthiopianDate('05-13-2018')` → `{day: 5, month: 13, year: 2018}` (Pagume 5 of a common year, accepted). The rule matches the real calendar: Wikipedia's Ethiopian-calendar article — the 6th epagomenal day is added on 29 August of the Julian calendar, six months before the corresponding Julian leap day (the leap year ends right before the leap day, i.e. the year before it); time.et/ertale — leap when the Ethiopian year leaves remainder 3 on division by 4 (2015, 2019 leap; 2018 E.C. is a common year). The rollover defect class is gone: no typed Pagume day can silently map to the next Ethiopian year's Meskerem 1. Fix confirmed; nothing further required.
  - Observation (no action) — `updateReport` strips an empty-string `generated` (`report.validator.js:59` uses `optional({ values: 'falsy' })`), so the API cannot clear generated text to empty; the UI has no clear action in Phase 3 and Generation arrives in Phase 5, so no behavior is reachable yet. Revisit when the Generate action lands.
  - Observation (no action) — `MuiSelect` (`client/src/components/reusable/MuiSelect.jsx`) has no caller in Phase 3; it is spec-catalogued (`## MUI Component Standards` §14.1) and Phase 4's recorder/branch flows may consume it — keep, do not delete.
- **Conflicts to be resolved:**
  - `OPEN` — **F-3-05 (note, Phase 8 alignment): the `updateBranch` archived-guard returns 409 while §35.4 requires 403 for non-lifecycle operations on archived resources, and `backend/utils/httpStatus.js` has no `FORBIDDEN` constant.** The guard (`backend/controllers/branch.controller.js:76–77`, `throw new CustomError(CONFLICT, 'Branch is archived')`) is unreachable in Phase 3 because no archive endpoint exists yet (archiving ships with the §35 lifecycle in Phase 8), so the conflict is not user-visible now and no user decision is needed today. Required for the implementation AI: no action in this fix round beyond keeping the guard as-is — the binding part is that Phase 8 must add `httpStatus.FORBIDDEN = 403` and switch this guard to 403 per §35.4 when the archive endpoints ship. Recorded here so it is not lost.
  - `RESOLVED` — **C-3-01 (user decision 2026-08-04): DD-MM-YY display with 2- or 4-digit typing is implemented.** Fix round 2: (1) `client/src/components/reusable/MuiDatePicker.jsx` now renders the display via `formatEthiopianDate` (DD-MM-YY, `ethiopianDate.js:145`) — read first-hand at lines 90–96, `formatEthiopianDate(gregorianToEthiopian(value.year(), value.month() + 1, value.date()))` — so the input shows DD-MM-YY exactly as REQ-110 / US-031 / §14.1.6 pin; (2) `parseEthiopianDate` now accepts `^(\d{2})-(\d{2})-(\d{2}|\d{4})$`, resolving a 2-digit year as `year + 2000` before the F-3-04 leap-year test. Verified numerically with the installed utilities: `parseEthiopianDate('30-02-18')` → `{day: 30, month: 2, year: 2018}` and `ethiopianToGregorian(2018, 2, 30)` → 2025-11-09 (the verified Gregorian anchor), so typing `30-02-18` commits 2025-11-09 and the field displays "30-02-18" — the round-trip is exact (probed: `formatEthiopianDate(gregorianToEthiopian(2025, 11, 9))` → "30-02-18"); `parseEthiopianDate('25-02-18')` → `{day: 25, month: 2, year: 2018}` → `ethiopianToGregorian(2018, 2, 25)` → 2025-11-04 → round-trips (`gregorianToEthiopian(2025, 11, 4)` → `{2018, 2, 25}`), displayed "25-02-18"; `parseEthiopianDate('06-13-2016')` → null (2016 is a common year — corrected per F-3-04; the review's original "leap year" label for 2016 was wrong, 2019 is the leap-year acceptance case: `06-13-2019` → `{day: 6, month: 13, year: 2019}`) and `parseEthiopianDate('07-13-18')` → null (common year 2018, day 7 > 5). The API strings (`formatEthiopianDateLong`, DD-MM-YYYY) are unchanged; the date filter, create dialog, and backend `?date` contract are unaffected. Fix confirmed; nothing further required.
- **Other required actions:**
  - `RESOLVED` — **Live backend smoke (process gap — the reason F-3-01 slipped through the first review).** Confirmed by re-verification: the fix round executed the required bounded single-shot backend smoke on the live DB (throwaway user, all probe data deleted afterward, port released) and recorded the evidence in the Phase 3 implementation-log entry — `POST /auth/register` 201 → `POST /branches` 201 → `POST /reports` 201 → `POST /audio` 201 → `GET /audio` 200 (`docs` + `totalDocs` — F-3-01 live-verified) → `GET /branches` 200 → `GET /reports` 200 → `GET /reports/:id` 200 with `status: "audio_attached"` and `audio[]`/`branches[].branchId` populated — the full Phase 3 chain (draft → audio_attached) works end to end. Action item complete; nothing further required in this round.
  - **Phase 4 business-logic integration analysis (user-requested — for the implementation AI to execute in Phase 4, after the Phase 3 fixes re-review GREEN):** every Phase 4 feature hooks into Phase 3 artifacts that already exist; the integration points are enumerated exhaustively here so Phase 4 can be implemented against them without re-deriving Phase 3 internals:
    - *Audio upload (physical file → metadata):* Phase 3's `createAudio` (`backend/controllers/audio.controller.js`) is metadata-first by design — its JSDoc states "Phase 4's upload endpoint attaches the physical file". Phase 4 adds the multipart upload (e.g. multer) on the existing `POST /` route of `backend/routes/audio.routes.js` (already validated by `backend/validators/audio.validator.js`: `reportId` isMongoId, `originalName`/`mimeType` non-empty, `fileSize` isInt ≥ 0, `duration` isFloat ≥ 0 — extend with the mime-type whitelist and file-size caps of `## Audio Recording STT` §2, keeping the `## API Contract` §10.7 envelope and 422 shape). The `filePath` field (default `''`) is ready to receive the stored path; files land under the gitignored `backend/uploads/audio/` (24h cleanup per §8.3 — the `archivedAt` TTL-index pattern in `branch.model.js` is the in-database precedent). The status-machine guard already exists: attaching audio to a `completed` report returns 409, and the report's `audio` array plus the draft → audio_attached transition are implemented; `Report.audio[]` is one-to-many, so multiple clips keep pushing and the single `transcription` ref is replaced by re-creating the transcription through the next point.
    - *STT pipeline (Addis AI):* Phase 3's `createTranscription` (`backend/controllers/transcription.controller.js`) is the STT sink — its JSDoc states "Phase 4's STT pipeline reuses this write path": it enforces the `audio_attached` state (409 otherwise), the one-transcription-per-report guard, and the audio_attached → transcribed transition; `Transcription.raw/latest/history` (with the `{ instruction, reviewed, reviewer, editedAt }` history-entry shape) are modeled and ready for the review workflow — `PATCH /api/v1/transcriptions/:id` already writes `reviewed` (`transcription.controller.js:96` guards the transcribed state). Phase 4 only calls the Addis STT endpoint (`## Audio Recording STT` §3–§4) and writes the result through `createTranscription`'s path — no schema or route changes expected.
    - *Recorder UI:* `CreateReportDialog` (`client/src/components/report/CreateReportDialog.jsx:30–33`) is documented as the Phase 3 metadata-only dialog ("the audio recorder and per-branch times arrive with Phase 4", `## UI/UX Spec` §11). Phase 4 adds the recorder section (MediaRecorder) into this dialog and converts the single global clockIn/clockOut into per-branch times — `Report.branches[].clockIn/clockOut` model fields already exist and `ReportDetails.jsx:102–113` already renders them; the reports list (`client/src/pages/Reports.jsx`) and details page (`ReportDetails.jsx:119–145`) already render audio metadata and only need playback/status affordances.
    - *Redux/client plumbing:* Phase 4 adds the audio list/create endpoints to `client/src/redux/features/` following the exact `branchSlice.js`/`reportSlice.js` injection pattern (`injectEndpoints` on the shared `api`, `transformResponse` unwrapping the envelope to `response.data`, `tagTypes` invalidation) and registers the new slice import in `client/src/redux/app/store.js` (side-effect import, per the existing comment); `PAGINATION_DEFAULT_*`/`PAGINATION_MAX_LIMIT` already exist for the audio list query.
    - *Testing:* Phase 4 must include DB e2e from the start (the gate missing in Phase 3): record → upload → STT → transcribed → reviewed, plus the 409 paths and the uploads TTL cleanup.
    - *Naming:* all Phase 4 docs/code keep the "V2" project naming per AGENTS.md.

### Phase 4 — Audio Recording And STT

- **Verdict:** GREEN
- **Findings:** Re-review of the complete fix round (the F-4-01..F-4-17 correction batch plus two follow-ups: the browser-reported multipart 422 and the dark/light submit overlay). Every one of the 17 findings is confirmed fixed and marked `RESOLVED` in place below; the two follow-ups are logged and resolved. The 422 defect (the reason for this re-review) is re-validated by a review-run live smoke against the final working tree: with the array-first `Audio.create([doc], { session })` form, a real `.webm` upload answers **201** and the report reaches `audio_attached` with the clip linked (populated `_id` match verified); the no-clips request answers 422 with the exact REQ-198 `errors: [{ field, message }]` array (`{ field: 'clips', message: 'At least one audio clip is required' }`); a bad MIME answers 415; a malformed `reportId` answers 422 and the route-level cleanup middleware unlinks the just-stored file (F-4-06 runtime-verified). Gates re-run on the final state: `node --check` on every backend file (PASS); client `npm run lint` (exit 0); one-shot `npx vite build` (0 errors, `client/dist/` deleted); `python scripts/verify-initial-doc.py` (exit 0, SELF-ALIGNED). Per-file audit of all eight modified backend files: no unused imports (scripted import-reference check), no `console.*`, `httpStatus` imports used (no raw numeric statuses), JSDoc intact. Throwaway smoke data deleted, the smoke-created clip unlinked, port 4005 released. Nothing remains open — the phase is per the specification and logically sound and can be merged after user approval (the fix round remains uncommitted, under user control). A full-codebase readiness sweep (Phases 1–4 → Phase 5) was also performed; the binding Phase 5 integration analysis in Other Required Actions stands.
- **Specification deviations:** (each finding: what was wrong / the violating spec section / the confirmed fix — status `RESOLVED`)
  - `RESOLVED` — **F-4-01 (HIGH): per-branch `MuiTimePicker` error text now renders.** `client/src/components/reusable/MuiTimePicker.jsx` (lines 26, 32–40) extracts `error`/`helperText` from the spread and merges them into `slotProps.textField` (mirroring `MuiDatePicker`). MUI X pickers have no top-level helperText handling, so the §6.1 "Error on respective MuiTimePicker" requirement is now met and the multi-branch silent dead end (previously an invisible no-op at `CreateReportDialog.jsx`) is gone. Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-02 (HIGH): details-page audio playback now authenticates.** `client/src/components/reusable/AudioClipRow.jsx` adds `crossOrigin="use-credentials"` to the `<audio>` element so the cross-origin `GET /audio/:id/stream` carries the httpOnly cookie (backend CORS is exact-origin + credentials, `backend/app.js`); without it Chrome blocks the media fetch as opaque and playback silently 401s. Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-03 (MEDIUM): `transcriptionSlice` is registered in `store.js`.** `client/src/redux/app/store.js` (line 19) now side-effect imports `../features/transcriptionSlice.js`, so `useTranscribeReportMutation`/`useUpdateTranscriptionMutation` (and the transcribe/update endpoints) exist for every consumer, not only the lazy `ReportDetails` chunk. Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-04 (MEDIUM): the env fail-fast probe no longer breaks the system-path default.** `backend/config/env.js` (lines 104–114) replaced the `existsSync` directory-relative probe with `execFileSync(binaryPath, ['-version'])` in try/catch — PATH/PATHEXT-aware, so the documented `'ffmpeg'`/`'ffprobe'` defaults keep resolving while a genuinely missing binary still fails loudly at boot instead of a cryptic upload-time 422. Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-05 (MEDIUM): every upload-422 carries the REQ-198 errors-array shape.** `backend/controllers/audio.controller.js` adds the `validationFailure(field, message)` helper (lines 49–53) and the empty-clips path returns the full `{ success:false, message:'Validation failed', data:{ errors:[{ field, message }] } }` envelope (lines 138–144). Two follow-ups closed the last mask of this class (the browser-reported 422): the catch block (lines 215–233) normalizes a Mongoose `ValidationError.errors` object-map to `[{ field, message }]` via `Object.entries(...)`, and `CreateReportDialog.jsx` resolves both the array and the object-map shapes before falling back to `error.data.message`. Fix confirmed; nothing further required.
- **Invalid business logic implemented:** none. Verified line by line on the final state: the status machine (draft → audio_attached on upload with the completed-409 guard, audio_attached → transcribed after STT, re-transcription accepting audio_attached/transcribed/reviewed and blocking only completed — `## Audio Recording STT` §9); the REQ-082/ADR-018 session transaction now genuinely covers the inserts (array-first `Audio.create([doc], { session })`; mongoose 9 treats a plain-object second argument as a second document — the F-4-07/422 trap); the 502 envelope echoing the report's real status; transcription overwrite with `raw` reset + `latest`/`history[]` reset (REQ-145) vs create-with-`latest: ''`/`history: []` (REQ-149); the 201 vs 200 distinction; `mapSttError` matching `## Addis AI` §12; the user-scoped list queries and envelope/422 shapes of the Phase 3 controllers unchanged.
- **Edge cases to be handled:** (each gap: the scenario, the confirmed fix — status `RESOLVED`)
  - `RESOLVED` — **F-4-06 (MEDIUM): a post-multer validator 422 no longer orphans the just-stored clip files.** `backend/routes/audio.routes.js` (lines 33–52, 59) inserts `cleanupUploadedOnFailure` between `receiveUpload` and `validateAudioUpload`: on a non-2xx `'finish'` it unlinks `req.files` unless the controller already initiated cleanup (`req.cleanedUploads`), so a bad-`reportId` 422 cleans its file without double-unlinking the controller's own 422s. Runtime-verified during the re-review smoke (no leftover file after the malformed-`reportId` request). Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-07 (MEDIUM): the multi-clip upload write is transactional (REQ-082).** `uploadAudioClips` runs the clip-create loop + `report.audio.push` + `draft` → `audio_attached` + `report.save` inside one MongoDB session (start/commit/abort/endSession). Follow-up (the browser-reported 422, re-validated this cycle): `Model.create(doc, { session })` treats `{ session }` as a second document in mongoose 9 — options are honored only with the array-first `create([doc], { session })`; now used (lines 181–194) and live-verified as 201 with the report `audio_attached`. The catch normalizes the ValidationError map and aborts the session. Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-08 (MEDIUM): an unknown-outcome network-error retry can no longer duplicate Audio docs.** `CreateReportDialog.jsx` sets `audioUploadAttempted` (lines 151–153) on network-error uploads; a re-entered Submit with an existing report skips the re-upload and proceeds straight to transcribe (line 355), and the 502 retry branch now gates on `audioUploaded` (line 371, F-4-17c). Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-09 (MEDIUM): `streamAudio`/`downloadAudio` attach a `createReadStream` error handler.** `stream.on('error', next)` is attached before `pipe` (`audio.controller.js:262–263,292–293`) and `backend/middleware/error.middleware.js` (lines 31–34) forwards via `next(err)` when `res.headersSent` so Express destroys the connection instead of a second response. Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-10 (MEDIUM): the 502 payload's `status` echoes the report's real status.** `transcription.controller.js` (line 88) returns `data: { reportId, status: report.status }` instead of the hardcoded `'audio_attached'` — correct on the re-transcription path where the report may already be transcribed/reviewed. Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-11 (MEDIUM): a stale cancel-discard `onstop` can no longer kill a new recording.** `useAudioRecorder.js` tags each session (`recordingSessionRef`, incremented in `startRecording`, snapshotted in `beginRecording`); a mismatched `onstop` is a no-op, and `discardRef` resets at the start of every `startRecording`. Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-12 (NOTE): addis.service defensive gaps closed.** (a) empty/whitespace transcription is a non-retryable error (`statusCode: 200`) — an all-empty result can no longer advance the report to `transcribed` with `raw: ''`; (b) the AbortController timeout is cleared in a `finally` spanning `fetch` + `response.json()` — a provider that answers headers then stalls the body aborts instead of hanging (REQ-129); (c) `splitWavChunks` throws when `byteRate <= 0`; (d) `ADDIS_AI_STT_MAX_BYTES_PER_REQUEST` (10 MB, REQ-128) is enforced with a non-retryable throw. Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-12c (NOTE): the splitter infinite-loop guard.** `backend/utils/wavSplitter.js` throws `TypeError('Invalid byte rate')` when `byteRate <= 0` and `TypeError('Invalid chunk size')` when the computed `chunkBytes <= 0`. Probe: byteRate 0 → TypeError; byteRate 32000 → 1 chunk. Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-13 (NOTE): dead code removed and the audit claim corrected.** `transcription.controller.js` drops the unused `totalChunks` accumulator and the un-triggerable `if (!clip.filePath) continue;` skip; the implementation-log "no unused vars" line is corrected in place (F-4-13 record). Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-14 (NOTE): `Audio.filePath` is now `required: true`** per `## Data Modeling` §4.2; the Phase 3 `default: ''` is removed (`audio.model.js:28`) and the controller no longer compensates for empty paths (the `transcribeReport` skip is gone — F-4-13; `resolveUploadPath` + 404-on-missing-file covers the rest). Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-15 (NOTE): `AudioClipRow` resets on natural end.** `onEnded` restores the play icon, rewinds `currentTime`, and resets the position to 0 (`AudioClipRow.jsx:60–69`). Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-16 (NOTE): details-page UX + reusable-component deviations fixed.** `ReportDetails.jsx` gates the full-page spinner on `isLoading` (background refetches from tag invalidation no longer blank the transcription editor) and the raw `TextField` became `MuiTextField` (`## MUI Component Standards` §8); MuiTextField forwards `multiline`/`minRows`/`fullWidth` through `...rest`. Fix confirmed; nothing further required.
  - `RESOLVED` — **F-4-17 (NOTE): create-dialog/draft-store state edge cases + conventions fixed.** (a) removing a branch filters `branchErrors` at the same index and selector `onApply` resets them — stale helper texts no longer mislabel rows; (b) `audioDraftStore.js` adds the `memoryAuthoritative` set: an IndexedDB write failure marks the key so `loadAudioDraft` reads the fresher memory tier instead of the stale IDB record; the mark clears on a successful write or clear; (c) the 502 branch gates on `audioUploaded`; (d) waveform magic values moved to `client/src/utils/constants.js` (`AUDIO_WAVEFORM_FFT_SIZE` 256, `BAR_WIDTH_RATIO` 0.6, `FALLBACK_WIDTH` 320, `CANVAS_HEIGHT` 64) and the submit overlay uses `rgb(${theme.vars.palette.background.paperChannel} / 0.85)` — MUI's per-scheme channel var resolves the correct paper color in both modes (dark-mode correct, no hardcoded color; the previous `alpha(theme.palette.background.paper, 0.85)` baked the static light value because the `cssVariables` theme resolves it per light scheme). Fix confirmed; nothing further required.
- **Conflicts to be resolved:** none needing a user decision — every finding above is closed. The recorded Phase 4 user decisions (split three-step create flow instead of the §6.2 single multipart request; deferred upload orphan sweep to Phase 8; indeterminate LinearProgress step overlays instead of progress events; single-branch global-times override; the unknown-outcome preservation semantics) are respected and are not findings; they stay as implemented.
- **Other required actions:**
  - `RESOLVED` — **Stale upload artifacts updated.** The re-review smoke deleted its throwaway user/branch/report/audio docs and unlinked its own uploaded clip; port 4005 released. Reference audit of the files under `backend/uploads/audio/`: `79b23e2c-2434-482b-b500-0e490e4d0600.webm` is referenced by an Audio document (kept); `e9772bb4-aae4-4d03-b195-4ae4d672cddb.webm` (the historical file previously labeled referenced by Audio doc `6a728082f76800bf721f3716`) and `f79b5f45-94a6-4f0c-b28b-33abe7cca81f.webm` are now **orphans** (their Audio docs were deleted during browser testing). Per the binding Phase 4 user decision the orphan sweep is deferred to Phase 8 — leave all three for the Phase 8 sweeper. Decided and recorded.
  - **Phase 5 business-logic integration analysis (user-requested precedent from Phase 3 — for the implementation AI to execute in Phase 5, after the Phase 4 re-review GREEN):** every Phase 5 feature hooks into existing Phase 3/4 artifacts; the integration points are enumerated so Phase 5 can be implemented against them without re-deriving internals:
    - *Generation endpoint:* Phase 5 adds `POST /api/v1/reports/:reportId/generate` (`ai.controller.js`) consuming `## Addis AI` §6 `chat_generate` (`POST {ADDIS_AI_BASE_URL}/api/v1/chat_generate`, `x-api-key`, `generation_config` from the frozen constants group — temperature 0.2, maxOutputTokens 2048, topP 0.9, topK 40). Phase 3 already models the output side: `Report.generated` + `generatedHistory[]` with the replacement semantics (`report.controller.js:140` area — previous text pushed as `{ provider, text }`, provider 400-required when replacing, AD-010), and `updateReport`'s `generated` validator rules exist. The 18 prompt seeds PR-01..18 of `## AI Prompt Spec` must be assembled as constants (`backend/utils/constants.js` — S-5-01b bans inline prompt text in controllers).
    - *ChatConversation model:* the Phase 1 review has a binding item — create `backend/models/chatConversation.model.js` **in Phase 5** (per T-5-02b and AD-010) and update the §4 tree at that point (REQ-173); `assistantApi.js` is already registered in the store (Phase 1) and `## Redux RTK Query` §3 inventories the `aiConversationSlice`.
    - *Review gate:* generation must run only from the reviewed transcription, never directly from raw audio (`## Transcription Review` §1, REQ-038) — guard on the transcription's `reviewed` flag / the report's status; Phase 3's `updateTranscription` PATCH already writes `reviewed` (verified this cycle: `transcription.controller.js:169–171`).
    - *Correction flow:* the Assistant chat (§11) drives the correction loop; the Edit action on ReportDetails (`reportColumns.jsx` onEdit) opens the report in the chat; generation/correction responses write through the same `Report.generated` replacement path with `provider` recorded (REQ-133).
    - *Errors/retries:* reuse `mapSttError`-style mapping for `chat_generate` errors (`## Addis AI` §12 table) and the mock-provider precedent (`mock-addis-stt.mjs`) extended to `chat_generate` for the S-5-xx validations; per-chunk retry behavior stays per `## Audio Recording STT` §5–9 and REQ-129.
    - *Naming:* all Phase 5 docs/code keep the "V2" project naming per AGENTS.md.
  - **MUI X Chat integration — Phase 4 → Phase 5 scope (user-mandated, binding for the implementation AI):** everything to be done with MUI X Chat in Phase 5 (client + backend) is logged here so Phase 5 can be implemented against it without re-deriving internals. The spec covers the facts below (`docs/specification.md`); anything **not** covered is listed under the gap below and **must** go through the user-question protocol before implementation.
    - *Covered — client:* §12.6 Assistant page (line 3197): `pages/Assistant.jsx`, route `assistant` (the only protected route outside AppShell, full-screen), `<ChatBox adapter={assistantAdapter} features={{ conversationList: true }} sx={{ height: '100vh' }} />`; conversation rail (title, last-message preview, relative timestamp); "New Chat" → report picker dialog → create conversation (`POST /api/v1/assistant/conversations` `{ reportId }`) whose welcome message injects the raw transcription + report metadata; conversation title `"Report {date}"`; deep link `/assistant?conversation=<id>` (ChatBox selects the conversation and shows its history — where the Reports-list "Edit" and ReportDetails "Edit Report" actions land; there is no report edit page); tool-approval UI built into ChatBox (Approve/Reject with reason; "expired" on 60s timeout); adapter file `client/src/components/assistant/chatAdapter.js` (plain JS object: `sendMessage`, `listConversations`, `listMessages`, `addToolApprovalResponse`); Redux `aiConversationSlice` (conversations list, activeConversationId, streaming parts) + RTK Query endpoints in `assistantApi.js` (`## Redux RTK Query` §3, REQ-103; tree lines 4265/4276/4283); package `@mui/x-chat` `^9.0.0-alpha.15` — already in the manifest, **community edition only** (§14.5 REQ-111); sidebar "Assistant" nav item (SmartToyIcon, highlighted on `/assistant`) per §12.3/§12.6.
    - *Covered — backend:* ChatConversation model (§24.9/§4.6, lines 2084–2099): `user` + `report` ObjectId refs (required), `title` default `"New Chat"`, `messages[]` shape `{ id (uuid), role ("user"|"assistant"), status ("streaming"|"complete"|"failed"), parts [Mixed], provider ("addis"|"gemini"|"nvidia"), createdAt }` with the four `parts` tool shapes (line 2095: `text` | `tool-input-available` | `tool-approval-request` | `tool-output-available`); index `{ user: 1, updatedAt: -1 }` (line 2098); cascade-deletes with the report (§35.2/ADR-018, supersedes the older "conversations kept" note; REQ-226). `save_transcription` tool semantics (line 2248): Transcription.latest + `history[]` push (`reviewer` = provider string) + Report status → `reviewed`, after user approval. Phase 5 `POST /reports/:reportId/generate` creates the ChatConversation record (T-5-02b/AD-010; no AIConversation/GeneratedReport collections) and records `provider` on messages (REQ-133). PR-01..18 delivered via `chat_generate` (T-5-01, frozen constants group).
    - *Not covered — gap (verified during this review; MUST be confirmed with the user before/while implementing):*
      1. **Assistant REST endpoints are absent from the spec's API Contract** — verified: `## API Contract` §8 (lines 2267–2294) covers only Report/Branch/Audio/Transcription/ChatConversation model shapes, no `assistant/…` routes. The full endpoint table (`GET/POST /api/v1/assistant/conversations`, `GET/POST /api/v1/assistant/conversations/:id/messages`, `POST /api/v1/assistant/tools/:toolCallId/approval`) exists **only** in read-only `docs/initial-doc.md` (lines 1969–1975). Per the source-of-truth hierarchy the implementation AI should confirm the endpoint set with the user and record the missing contract rows in the spec before Phase 5.
      2. **SSE streaming wire contract exists only in initial-doc** (lines 1977–2046): SSE event flow (`tool-input-available` → `tool-approval-request` → approval → `tool-output-available` → `finish`), in-memory pending map keyed by `toolCallId`, 60s approval expiry, `res.flush()` per write, keep-alive heartbeat every 15s. An assistant SSE section is **missing** from the spec — the wire (SSE vs JSON) must be defined/confirmed with the user.
      3. **Adapter wire-format gap:** the installed `@mui/x-chat` / `@mui/x-chat-headless` `9.0.0-alpha.15` `ChatAdapter` type (`client/node_modules/@mui/x-chat-headless/adapters/chatAdapter.d.ts`) requires `sendMessage(input) → Promise<ReadableStream<ChatMessageChunk | ChatStreamEnvelope>>` with chunk types `start / text-start / text-delta / text-end / tool-input-start / tool-input-available / tool-approval-request / tool-output-available / finish / abort` (`types/chat-stream.d.ts`); `listConversations → { conversations: ChatConversation[] }` with `{ id, title, subtitle, lastMessageAt }`; `listMessages({ conversationId }) → { messages: ChatMessage[] }` with `{ id, role, parts, status, createdAt }` (`types/chat-entities.d.ts`). The adapter must translate the backend response (SSE bytes or JSON) into these shapes — the mapping and message/part id conventions are a user-confirmed design decision.
      4. **No implementation-phase task builds the ChatBox:** §12.6 specs the Assistant page but none of T-5-xx..T-8-xx task items assign the ChatBox/chatAdapter/Assistant page work — the implementation AI must assign it to a phase (or add a placeholder task) and confirm with the user.
      5. **`aiConversationSlice` streaming-parts ownership is unspecified:** the slice inventory (`## Redux RTK Query` §3) lists conversations list + `activeConversationId` + streaming parts, but how those synchronize with ChatBox's internally streamed state is undefined — decide with the user.
      6. **Open UX confirmations for the user:** one conversation per report (find-or-create) vs many; behavior when the deep-linked conversation id does not exist; multi-conversation disambiguation in the rail; whether generation/correction is exercised through the chat (`save_transcription` + regenerate) or a separate flow.
    - *Required protocol (user-mandated):* because the spec does not cover items 1–6, the implementation AI MUST (1) exhaustively analyze MUI X Chat — the installed package API (components: ChatBox, ChatComposer, ChatConversation, ChatConversationList, ChatMessage*, ChatSuggestions; the `ChatAdapter`/`createEchoAdapter` types; `ChatMessageChunk | ChatStreamEnvelope` union; community feature set per §14.5 REQ-111) and its real capabilities/limitations; (2) reason over every project aspect for MUI X Chat utilization — the report-correction loop, `save_transcription`, `history[]` reviewer/provider display, store integration, deep-link entry from Reports/ReportDetails, provider indicator; (3) brainstorm and **ask the user** the open questions (gap items 1–6 and any new ones) before implementing; (4) implement based on the answers, then record each decision + its user answer as RESOLVED in the Phase 5 review.
    - *Reference:* this bullet complements — does not overwrite — the Phase 5 business-logic integration analysis above (generation endpoint, ChatConversation model creation in Phase 5, review gate, correction flow, errors/retries, naming).

- **Verdict:** PENDING
- **Findings:** (none yet — filled at the first review of Phase 5)
- **Specification deviations:** (each finding: what is wrong / the violating spec section and evidence / exactly what the implementation AI must do — status `OPEN`)
- **Invalid business logic implemented:** (only if any already implemented: the logic, why it is invalid, what the implementation AI must do)
- **Edge cases to be handled:** (gaps the implementation AI must cover: the scenario, the expected behavior, what the implementation AI must do)
- **Conflicts to be resolved:** (contradictions between implementation and spec, between documents, or within the implementation itself: the conflicting positions, the required resolution, and whether a user decision is needed before the implementation AI can act)
- **Other required actions:** (anything else the implementation AI must do)

### Phase 6 — Export And Analytics

- **Verdict:** PENDING
- **Findings:** (none yet — filled at the first review of Phase 6)
- **Specification deviations:** (each finding: what is wrong / the violating spec section and evidence / exactly what the implementation AI must do — status `OPEN`)
- **Invalid business logic implemented:** (only if any already implemented: the logic, why it is invalid, what the implementation AI must do)
- **Edge cases to be handled:** (gaps the implementation AI must cover: the scenario, the expected behavior, what the implementation AI must do)
- **Conflicts to be resolved:** (contradictions between implementation and spec, between documents, or within the implementation itself: the conflicting positions, the required resolution, and whether a user decision is needed before the implementation AI can act)
- **Other required actions:** (anything else the implementation AI must do)

### Phase 7 — Mock Data And Hardening

- **Verdict:** PENDING
- **Findings:** (none yet — filled at the first review of Phase 7)
- **Specification deviations:** (each finding: what is wrong / the violating spec section and evidence / exactly what the implementation AI must do — status `OPEN`)
- **Invalid business logic implemented:** (only if any already implemented: the logic, why it is invalid, what the implementation AI must do)
- **Edge cases to be handled:** (gaps the implementation AI must cover: the scenario, the expected behavior, what the implementation AI must do)
- **Conflicts to be resolved:** (contradictions between implementation and spec, between documents, or within the implementation itself: the conflicting positions, the required resolution, and whether a user decision is needed before the implementation AI can act)
- **Other required actions:** (anything else the implementation AI must do)

### Phase 8 — Quality Gates And Polish

- **Verdict:** PENDING
- **Findings:** (none yet — filled at the first review of Phase 8)
- **Specification deviations:** (each finding: what is wrong / the violating spec section and evidence / exactly what the implementation AI must do — status `OPEN`)
- **Invalid business logic implemented:** (only if any already implemented: the logic, why it is invalid, what the implementation AI must do)
- **Edge cases to be handled:** (gaps the implementation AI must cover: the scenario, the expected behavior, what the implementation AI must do)
- **Conflicts to be resolved:** (contradictions between implementation and spec, between documents, or within the implementation itself: the conflicting positions, the required resolution, and whether a user decision is needed before the implementation AI can act)
- **Other required actions:** (anything else the implementation AI must do)

# Implementation Log — What Was Implemented, Changes, Updates, And Corrections

> This log is the **implementation record** of the implementation phases of Report Builder V2: for every phase it records **what was implemented** (the deliverable the review AI validates against) and the **changes/updates/corrections** made. It is created by the phase protocol (Step 6 Post-Git, `## Phase Protocol` §7 of `docs/specification.md`) and is read in Steps 2–3 of every phase and in the review cycle (`use docs/review-implementation.md <N>`). **Every recorded item is binding: every future implementation phase respects it, and the review AI validates the phase against it.**

## Standing Guidelines (Binding)

> User-level development principles (2026-08-03, Phase 3). They rank with the user's direct instructions at the top of the source-of-truth hierarchy — read and respect them in every phase. New guidelines of this kind are appended to this section (they also live at the top of `docs/build-implementation.md`).

1. **React StrictMode is never the culprit and is never disabled or worked around.** StrictMode double-invocation (dev mounts/effects) must never produce duplicate responses — double network requests, double refreshes, double submits, or double toasts must be impossible in **any** case. Guarantees come from first-class mechanisms only: RTK Query per-endpoint dedup, the shared module-level refresh mutex in `baseQueryWithReauth` (`client/src/redux/features/api.js`), `loading`/`isSubmitting`-disabled submit buttons, and render-time state sync instead of effects where an effect would double-run.
2. **Verification uses only fast commands.** `node --check`, `npm run lint`, one-shot `npx vite build`, `python scripts/verify-initial-doc.py`, and bounded single-shot live smokes (seconds) are the only verification tools. Long-running commands or automated test suites are never used to test one thing — that is exactly why automated tests were dropped.

## How To Use This Log

- At the end of every phase (Step 6 Post-Git, action 1), the implementation AI **FIRST** records the phase below — what was implemented plus the changes/updates/corrections — and **SECOND** aligns the docs and specifications.
- At the start of every phase (Steps 2–3), the implementation AI reads this log and applies every recorded item in the new phase.
- The review AI reads the **Implemented** summary to know exactly what was delivered and what to validate in the review cycle.
- Each entry: phase number, branch, commit, date, what was implemented, the changes/updates/corrections made (what was changed, why, and what it affects), and the validation results recorded for the phase.

## Log

### Phase 1 — Foundation

- **Branch:** `phase-1-foundation` — **Commit:** `feat: phase 1 foundation`
- **Date:** 2026-08-03
- **Implemented:**
  - Backend scaffold: `config/env.js` (frozen env, 21 required keys with spec defaults, throws on missing keys/JWT secrets < 32 chars, sole `process.env` access point, optional OAUTH*GOOGLE*\*), `config/db.js` (mongoose connect + lifecycle loggers), `app.js` (fixed security stack `helmet -> cors -> compression -> cookie-parser -> makeQueryWritable -> mongo-sanitize -> rate-limit`, morgan dev-only, `/api/v1` routes, notFound + errorHandler terminals), `server.js` (listen-before-DB per REQ-084, graceful shutdown `server.close() -> mongoose.connection.close() -> process.exit(1)` with 30s force-exit timer, `shuttingDown` guard).
  - `middleware/`: `authenticate.middleware.js` (cookie accessToken + JWT verify + User lookup → `req.user`, 401 envelope), `notFound.middleware.js` (CustomError(404) → next), `error.middleware.js` (operational CustomError → statusCode; CastError → 400, ValidationError → 422, duplicate key 11000 → 409, JWT errors → 401; unexpected logged with stack+status, generic message in production, stack in development).
  - `models/` (all with `timestamps: true` + DM-03 toJSON/toObject transforms deleting `id`/`__v`; User additionally deletes `password` and keeps `virtuals: true`): `user.model.js` (email unique index via `schema.index`, bcrypt 12-round pre-save when modified, `comparePassword`, `fullName` virtual), `branch.model.js` (unique `{user, name}` index, 30-day TTL partial archive index, paginate plugin), `report.model.js` (`{user, createdAt}` + `{status}` + TTL indexes, paginate plugin), `audio.model.js` (no status field), `transcription.model.js` (paginate plugin).
  - `validators/`: shared `validation.js` (express-validator check → 422 `{ success: false, message: 'Validation failed', data: { errors: [{ field, message }] } }`, `req.validated = matchedData`) + 8 per-domain files `auth.validator.js`, `branch.validator.js`, `report.validator.js`, `audio.validator.js`, `transcription.validator.js`, `ai.validator.js`, `user.validator.js`, `analytics.validator.js` (`<domain>.validator.js` naming per REQ-176).
  - `routes/`: `index.js` (mounts all 8 domain modules under `/api/v1` + `/health` envelope) + per-domain route files with `/health` probes and temporary `/validate` scaffolds (replaced by real contracts in Phase 2); `auth.routes.js` additionally exposes `/protected` (authenticate).
  - `utils/`: `constants.js` (frozen, incl. `MONGOOSE_DUPLICATE_KEY_ERROR_CODE`, `SHUTDOWN_FORCE_EXIT_TIMEOUT_MS`, `ARCHIVE_TTL_SECONDS`, pagination defaults, BCRYPT_SALT_ROUNDS), `httpStatus.js`, `error.js` (CustomError), `logger.js` (Winston, daily-rotated gitignored `logs/`, child loggers, printf emits `status=NN` + stack metadata).
  - Frontend shell: `redux/app/store.js` (configureStore with api + assistantApi), `redux/features/api.js` (createApi + `baseQueryWithReauth` — 401 → `/auth/refresh` retry, failure → clear + logout), `redux/features/assistantApi.js`, `components/layout/AppErrorBoundary.jsx` + `AppToastContainer.jsx`, `utils/constants.js` (API_CONFIG from `import.meta.env`), `utils/ethiopianDate.js` (Gregorian↔Ethiopian JDN algorithms + `formatEthiopianDate`, verified roundtrips), `App.jsx` (AppTheme → CssBaseline → AppErrorBoundary → AppToastContainer → Outlet), `main.jsx` (Provider → LocalizationProvider/AdapterDayjs → RouterProvider with 11 lazy routes incl. `*` NotFound), 11 lazy page shells.
  - Endpoints verified in smoke tests: `GET /api/v1/health` 200; `GET /api/v1/auth/protected` 401; unknown route 404 envelope; `POST /api/v1/auth/validate` 422 shape + 200; `GET /api/v1/analytics/validate?page=abc` 422.
- **Changes/updates/corrections:**
  - Engine-compat fixes (documented as codebase facts): `express-mongo-sanitize` 2.2.0 assigns to `req.query`, which Express 5 exposes as a getter-only accessor → `makeQueryWritable` shim (Object.defineProperty) inserted between cookie-parser and mongo-sanitize; `jsonwebtoken` v9 has no named ESM exports → default import; Mongoose 9 `connection.close()` is promise-only (no callback).
  - Review round 1 (from `docs/implementation-review-log.md` Phase 1): added DM-03 (`timestamps: true` + toJSON/toObject transforms) to all five models; renamed the 8 validators to `<domain>.validator.js` and updated route imports; logger printf now emits `statusCode` + `stack` metadata (REQ-196); added `mongoose-paginate-v2` to Transcription (DM-04); documented `makeQueryWritable` in `## Backend Architecture` §2 and `## Security` §5; recorded `validators/validation.js` in the `## Project Directory Structure` §4 tree; fixed the T-1-04d REQ-085→REQ-086 spec typo.
  - Review round 2 (from `docs/implementation-review-log.md` Phase 1): removed the invented password `min: 6` rule from `validateRegister` in `backend/validators/auth.validator.js` (replaced with a `notEmpty()` presence check matching `validateLogin`) — the rule had no spec basis and violated REQ-083 (constants must live in `utils/constants.js`); the final register validation rules are Phase 2 scope (T-2-01b).
  - Fixes from verification: `jdnToGregorian` in `client/src/utils/ethiopianDate.js` used a wrong inverse formula — replaced with the Fliegel–Van Flandern algorithm (named constants, no magic values); `main.jsx` lazy route entries required `/* eslint-disable react-refresh/only-export-components */` (entry file, no exports — false positive).
- **Validation results:**
  - `node --check` on every backend file: PASS.
  - `npx vite build`: 0 errors (chunk-size warning only, expected with MUI); `client/dist/` deleted after each build.
  - `npm run lint` (client): PASS, exit 0.
  - `python scripts/verify-initial-doc.py`: exit 0, SELF-ALIGNED.
  - Grep audits: no `process.env` outside `config/env.js`; no `console.log` in backend source; `git check-ignore` confirms `backend/.env`, `backend/logs/`, `backend/uploads/audio/`.
  - Ethiopian date util: 10 anchor cases (incl. Pagume month 13, New Year, leap boundaries) all roundtrip OK.
  - Boot smoke tests: all probes GREEN (health 200, protected 401, 404, 422 validation shape, 200 validate); graceful shutdown verified (`SIGINT` → server closed → DB disconnected → exit, port released).
  - Round-2 probe re-verification: `POST /api/v1/auth/validate` with short password + bad email → 422 (email error only); short password + valid email → 200; empty password → 422 ("Password is required"); health 200; `node --check` PASS; no `isLength`/`min: 6` remnants anywhere in backend source.
  - DM-03 runtime check: `timestamps` on, paginate plugin on Report/Transcription, transforms strip `id`/`__v` (+ `password` on User), `fullName` virtual preserved.
  - Logger metadata check: error entries render `status=500` + full stack in `backend/logs/` and console.

### Phase 2 — Authentication And User Management

- **Branch:** `phase-2-authentication-and-user-management` — **Commit:** `feat: phase 2 authentication and user management`
- **Date:** 2026-08-03
- **Implemented:**
  - Backend auth (T-2-01, `## Auth Cookies` §11, API Contract §4, REQ-087/088/089/091): `backend/validators/auth.validator.js` final rules — email trim/normalize/`.isEmail()`, password `notEmpty()` + `.isLength({ min: PASSWORD_MIN_LENGTH })`; `backend/controllers/auth.controller.js` — `register` (201, `extractNamesFromEmail` per §11.4: dotted local part splits into firstName/lastName, non-dotted repeats as both; 409 on duplicate), `login` (200; `.select('+password')` + `comparePassword`; 401 otherwise), `logout` (idempotent; verifies refresh cookie, nulls `user.refreshToken`, clears both cookies), `refresh` (rotation per REQ-087: mismatch/replay/tamper → 401 + null; re-issues both cookies), `me` (200), `googleOAuthStart`/`googleOAuthCallback` (stubbed flow); helpers `issueTokens`, `setAuthCookies`/`clearAuthCookies` (access cookie name `accessToken`, path `/`, 15m; refresh cookie name `refreshToken`, path `/api/v1`, 7d; both `httpOnly`, `sameSite: 'lax'`, `secure` only in production), `extractNamesFromGoogleProfile`.
  - `backend/services/oauth.service.js` (T-2-02): `getGoogleOAuthUrl`/`exchangeCodeForProfile`/`isGoogleOAuthConfigured`; scopes `openid, email, profile, https://www.googleapis.com/auth/drive.file` (REQ-158, AD-012); 503 `CustomError` while any `OAUTH_GOOGLE_*` is `"change me"` or missing; token persistence deliberately deferred to Phase 6.
  - `backend/routes/oauth.routes.js`: `GET /oauth/google` + `GET /oauth/google/callback`, **mounted at root level in `app.js` (`app.use('/oauth', ...)`) — user-approved decision overrides the stale `.env` `OAUTH_GOOGLE_CALLBACK_URL`**.
  - `backend/routes/auth.routes.js`: real contract — `POST /register`, `POST /login` (auth rate tier 20/15min), `POST /logout`, `POST /refresh`, `GET /me` (authenticate).
  - `backend/controllers/user.controller.js` + rewritten `backend/routes/user.routes.js`: `GET /user/profile`, `PUT /user/profile` (firstName/lastName/avatar/position; both authenticate; PUT also `validateUpdateProfile`).
  - Backend constants: `COOKIE_ACCESS_TOKEN`, `COOKIE_REFRESH_TOKEN`, `COOKIE_ACCESS_MAX_AGE_MS` (15m), `COOKIE_REFRESH_MAX_AGE_MS` (7d), `PASSWORD_MIN_LENGTH: 6`, `OAUTH_GOOGLE_PLACEHOLDER: 'change me'`; `httpStatus.js` += `SERVICE_UNAVAILABLE: 503`.
  - Frontend auth state (T-2-03, `## Redux RTK Query` §2, REQ-103/104/105): `redux/features/authSlice.js` — `authApi` injectEndpoints (register/login/logout/me) + `createSlice` with `setCredentials`/`logout`, `initialState` from `readStoredUser()` (`initializing: true`), matchers sync Redux + `localStorage['auth_user']` on register/login/me fulfilled, clear on me rejected and logout fulfilled; `store.js` registers the slice; `api.js` `baseQueryWithReauth` skips the refresh retry for `AUTH_NO_REFRESH_PATHS` (`/auth/login`, `/auth/register`, `/auth/refresh`), clears only the auth key and dispatches `auth/logout` on refresh failure.
  - Frontend layout + guards (T-2-04/05, `## Routing Layout`, REQ-095/096/097): `AppErrorPage.jsx`, `PublicLayout.jsx` (100vh/overflow hidden, fixed public MuiAppbar + `marginTop: 8` scrollable main), `AppShell.jsx` (100vh/overflow hidden; AppSidebar + content column with static protected MuiAppbar 64px INSIDE the content area — app bar never spans the sidebar; floating menu icon on mobile opens the drawer), `AppSidebar.jsx` (temporary overlay 240px below `md`; permanent docked at `md+`, full 240px / mini 64px with header menu toggle; nav Dashboard/Reports/Branches/Profile/Assistant; selected = `alpha(primary, 0.08)` + 3px primary left border; bottom divider + Logout via `useLogout`), `PublicRoute.jsx` / `ProtectedRoute.jsx` (spinner while `initializing || isFetching` from `useGetMeQuery`, `<Navigate to="/login" state={{ from: location }}>`; inverse for public, `<Navigate to="/dashboard">`), `hooks/useLogout.js` (mutation → catch-dispatch `logout()` → navigate `/login`), `main.jsx` guarded tree: App → PublicRoute → PublicLayout (index/login/register) and ProtectedRoute → AppShell (dashboard/reports/reports/:id/details/branches/branches/:id/details/profile) + `assistant` as AppShell sibling + `*` NotFound under ProtectedRoute.
  - Frontend reusable components (T-2-06, `## MUI Component Standards`): `MuiButton.jsx` (size small default, native `loading` + 20px CircularProgress), `MuiTextField.jsx` (forwardRef, internal password eye without layout shift, merges caller `slotProps.input.endAdornment`), `MuiDialog.jsx` (title/divider/content/actions skeleton, responsive fullscreen `down('sm')` or `down('md')` landscape, `disableEnforceFocus`/`disableRestoreFocus`), `LoadingSpinner.jsx`, `GoogleIcon.jsx` (18×18 inline SVG), `MuiAppbar.jsx` (public/protected variants; logo → `/dashboard` if authenticated else `/`; ThemeToggle via `useColorScheme`; public unauth: Login + Sign Up buttons, auth: Logout icon; protected: Search → GlobalSearchDialog, ThemeToggle, avatar 32px (<600px)/36px (≥600px) → dropdown Profile/Logout), `GlobalSearchDialog.jsx` (UI shell per user decision — RHF input, back-arrow reset+close, grouped Reports/Branches Accordions, "No results found" empty state; fullscreen <600 or <768 landscape, width 600/720, height 80vh/70vh; results wiring deferred to Phase 3).
  - Login/Register (T-2-07, `## Auth Pages` §3): `components/login/LoginForm.jsx` + `components/register/RegisterForm.jsx` (RHF `mode: 'onBlur'`; 422 → `setError`, 401/409/429 → field errors or toasts; loading via `isSubmitting`; success → reset + navigate to `location.state.from` or `/dashboard`; Google outlined button with GoogleIcon, stub-redirects to `/oauth/google`), `pages/Login.jsx`/`Register.jsx` thin wrappers (centered `Paper elevation={3} maxWidth 420`).
- **Changes/updates/corrections:**
  - **Bug found during live e2e verification (Phase 1 code):** `user.model.js` pre-save hook was `async function hashPassword(next)` calling `next()` — Mongoose passes no callback to promise-returning hooks → `TypeError: next is not a function`, register 500'd. Fixed to a plain async hook (no `next` param). This is the first runtime execution of the register flow (Phase 1 never ran it against a DB).
  - **Security fix found during e2e:** the User toJSON/toObject transforms did not delete `refreshToken`, so register/login/me responses leaked the refresh token to the client. Both transforms now delete `ret.refreshToken` (DB storage untouched; rotation replay check still reads the doc).
  - **Frontend runtime fix:** `useMediaQuery` is exported from `@mui/material` (root), NOT `@mui/material/styles`, in the installed MUI version — corrected in `MuiAppbar`, `MuiDialog`, `GlobalSearchDialog`, `AppSidebar`, `AppShell`; `vite build` then passed.
  - **User decisions (binding):** (1) Phase 1 merged to `main` first, Phase 2 branched from `main`; (2) full spec-compliant layouts, no minimal skeletons; (3) GlobalSearchDialog ships as UI shell, results Jstubbed until real credentials; (5) name extraction: §11.4 wins over REQ-170 — non-dotted email local part repeats as both names (`beza@gmail.com` → firstName `beza`, lastName `beza`); (6) OAuth routes mounted at root `/oauth/google` (not `/api/v1/...`); `.env` `OAUTH_GOOGLE_CALLBACK_URL` left as-is (placeholder).
  - **Mobile sidebar trigger:** spec §2.3 keeps the hamburger out of MuiAppbar; on <md the AppShell renders a floating menu icon over the appbar's logo position to open the temporary drawer (documented as Phase 2 decision; revisit if the review flags it).
  - **Review round 1 (from `docs/implementation-review-log.md` Phase 2, verdict FAIL — fix loop):**
    - `main.jsx` route objects: swapped `element: <... />` for `Component: ...` on the four pathless wrappers (PublicRoute, PublicLayout, ProtectedRoute, AppShell) per REQ-094 (no `element:` usage); added `ErrorBoundary: AppErrorPage` to the root route object (§12.5) — `AppErrorPage` is now live instead of dead code; moved `{ path: '*', Component: NotFound }` into AppShell's children so the 404 renders inside the shell chrome (§12.6); `assistant` remains the only protected route outside AppShell.
    - Barrel imports removed per REQ-107/§14.1: `useMediaQuery` now imported as the default export from the deep path `@mui/material/useMediaQuery` (verified the installed MUI 9.2.0 exports `./useMediaQuery`) in `MuiDialog.jsx`, `MuiAppbar.jsx`, `GlobalSearchDialog.jsx`, `AppSidebar.jsx`, `AppShell.jsx`; `useTheme` split back to `@mui/material/styles` in AppSidebar/AppShell. The Phase 2 "runtime fix" note claiming the barrel was forced was wrong — corrected.
    - `GlobalSearchDialog.jsx` now composes `MuiDialog` (REQ-109/§14.3) instead of raw `@mui/material/Dialog`; `fullScreen`/`onClose`/`PaperProps` pass through; spec §12.3 breakpoints (600px / 768px landscape) preserved; header row and results stay as children in the scrollable DialogContent.
    - Added **DM-08** to `## Data Modeling` §3 (Phase 1 review deferred action): "Required model fields use plain `required: true`; custom validation messages are defined in the express-validator (`withMessage`) and RHF layers — never in model schemas."
  - **Review round 2 (from `docs/implementation-review-log.md` Phase 2 — 5/5 round-1 findings confirmed RESOLVED; 2 new findings):**
    - Landing page implemented per spec §12.6 §6 (user decision: the basic landing page must be built in this phase, not left as the Phase 1 shell): `client/src/pages/Landing.jsx` now renders the static hero — centered wrapper `max-width: 1200px`, `Description` logo/icon, headline "Build Better Reports" (h4 on xs, h3 on md+ via responsive `sx` typography), subheadline "Record, transcribe, and generate professional reports with AI", CTAs "Get Started" (contained → `/register`) and "Sign In" (outlined → `/login`) via `MuiButton` + `useNavigate()`; no data fetching, no Redux, text `noWrap` (ellipsizes on overflow), no horizontal scroll.
    - `textAlign` DOM-prop warning removed per REQ-107/§14.1 (sx only): `LoginForm.jsx` and `RegisterForm.jsx` now set `sx={{ mt: 2, textAlign: 'center' }}` instead of the forwarded `textAlign="center"` Typography prop (React warning "React does not recognize the `textAlign` prop"); the `Divider textAlign="center"` labeled-divider API usages are untouched (legitimate MUI v9 Divider prop, per review).
  - **User-reported fixes (public MuiAppbar, initial-doc §1.1):** the spec's left section is one unit — "Logo icon + app name. Click navigates to `/dashboard` if authenticated, `/` if not" — but the Phase 2 build rendered them as separate elements (only the small icon was clickable; the name showed no pointer and did nothing on click). `MuiAppbar.jsx` now renders the public left section as a single `Button variant="text" color="inherit"` (`aria-label="App home"`, `cursor: pointer` natively, `onClick` → `/dashboard` when authenticated else `/`) containing the `Description` icon + app-name `Typography variant="h6" component="span"`; the name is hidden on mobile portrait (`display: { xs: 'none', sm: 'block' }`, `noWrap`) so the toolbar fits the theme toggle + Login + Sign Up on small screens; the protected variant keeps its icon-only logo per spec (no title in the protected bar). `PublicLayout.jsx` content offset corrected for mobile: `marginTop: { xs: 7, sm: 8 }` (56px mobile / 64px desktop toolbar heights) so content never hides under the fixed app bar.
  - **User decision (icon sizing):** no fixed-pixel icon sizes anywhere — every MUI icon uses a proper MUI prop. Functional icons (`Menu`, `Description`, `Dashboard`, `Storefront`, `Person`, `SmartToy`, `Logout`, `LightMode`, `DarkMode`, `Search`, `Visibility`, `VisibilityOff`, `ArrowBack`, `ExpandMore`, `Email`, `Lock`) all render `fontSize="small"`; the three logo icons (Landing hero, Login/Register card logos) use `fontSize="large"` + `color="primary"` instead of the previous `sx` fixed numbers (64/48px). `GoogleIcon` (custom 18px inline SVG) untouched — not a MUI icon.
  - **User-reported runtime fix (`useGetMeQuery is not a function`):** user hit `TypeError: useGetMeQuery is not a function` at `ProtectedRoute.jsx:21` after a successful sign-up (backend log showed `login 401` then `register 201` — register/login/logout mutations worked, only the query hook crashed). Root cause: RTK Query names hooks from the endpoint name — endpoint `me` generates `useMeQuery`, **not** `useGetMeQuery` — so the destructured `export const { useGetMeQuery, ... } = authApi` silently captured `undefined`. Fixed by renaming the endpoint `me` → `getMe` in `redux/features/authSlice.js` (URL stays `GET /api/v1/auth/me`; `authApi.endpoints.me.matchFulfilled/matchRejected` → `authApi.endpoints.getMe.*`); the `useGetMeQuery` export and `ProtectedRoute.jsx` usage now resolve correctly. Confirmed via node repro on the installed RTK 2.12.0: endpoint `me` → `useMeQuery` only.
  - **MUI 9.2.0 system-prop leak on `Box` (`flexDirection`/`alignItems`/`justifyContent`/`minHeight` DOM warnings):** direct MUI system props are no longer consumed by `Box` in MUI 9.2.0 — they leak onto the DOM element (React "does not recognize" warnings) and produce **no styling**. User reported the warnings while registering; reproduced in isolation via SSR (`<div ... display="flex" flexDirection="column" ...>` with empty `css-0` class). `LoadingSpinner.jsx` moved all five props into `sx` — verified zero warnings + full CSS emitted (this also fixed a hidden visual bug: the spinner was never actually centering).
  - **Protected MuiAppbar logo removed (user decision):** the `Description` icon-only logo IconButton was deleted from the protected app bar variant; the left of the protected bar is now empty (the AppShell floating hamburger occupies that spot on mobile; Search/theme/avatar stay on the right). Public variant unchanged.
  - **AppSidebar logo clickable (user request):** `Description` + app name are now a single `Button variant="text" color="inherit"` (`aria-label="App home"`, native pointer, `onClick` → `/` + drawer `onClose()`); the app name uses `API_CONFIG.VITE_APP_NAME` instead of the hardcoded "Report Builder" (matches MuiAppbar, removes a magic value). The toggle IconButton stays separate. Note: the click itself always worked — `PublicRoute` was bouncing authenticated users off `/`; see the guard change below.
  - **AppSidebar nav spacing + mini-mode fix (user report):** the theme's `MuiList` styleOverride makes every List a flex column (`customizations/dataDisplay.js`) and MUI 9.2.0's `ListItemButton` hardcodes `flex-grow: 1` (verified in node_modules) — together they stretched every nav item across the sidebar's full height in full and mini modes. Nav items and the Logout item now get `flex: '0 0 auto'`; mini mode `ListItemIcon minWidth` 40 → 16 (the 40px box pushed the 16px glyph 12px off-center in the 64px drawer: 64 − list padding 16 − item gutters 32 = 16px of content).
  - **`PaperProps` → `slotProps.paper` (MUI 9.2.0 dialog API):** user reported "React does not recognize the `PaperProps` prop". MUI 9.2.0's `Dialog` no longer has a `PaperProps` prop (only `slotProps`; verified `useSlot('paper')` in node_modules), so it leaked to the root DOM element through `MuiDialog`'s `...rest`. `GlobalSearchDialog.jsx` now passes `slotProps={{ paper: { sx: {...} } }}`; verified styles land on the paper element.
  - **Avatar menu styling (user request):** Profile/Logout items now render `Person`/`Logout` icons via `ListItemIcon` (`minWidth: 32` — the theme zeroes the ListItemIcon default) + `ListItemText`.
  - **GlobalSearchDialog sizing + clear (user requests, iterative):** desktop height 70vh → 80vh (top position per user: fine); large-viewport cutoff `up('lg')` → `up('md')`; top padding 36px → 8px via a new `contentSx` prop on `MuiDialog` (`DialogContent sx={{ overflowY: 'auto', ...contentSx }}`, JSDoc updated) + form `pt: 1`; a `Close` clear button appears left of the Search submit button while the field has text (clears value + results). Value read via `useWatch` instead of `watch` — the project's React Compiler eslint rule (`react-hooks/incompatible-library`) flags RHF `watch` as non-memoizable. **Width final value: 600px — set manually by the user** (history: 720 → 800 → 760 per user feedback; the user settled on 600 as "ok").
  - **PublicRoute Landing exception (user decision via question):** authenticated users may now view `/` (the Landing page) but **not** `/login`, `/register`, or any future public page. Guard is pathname-based: `if (isAuthenticated && location.pathname !== '/') return <Navigate to="/dashboard" replace />`. This makes the sidebar logo → `/` navigation actually land (previously the guard bounced it back to `/dashboard`); the public app bar logo (authenticated → `/dashboard`) is the way back.
  - **Review round 3 (from `docs/implementation-review-log.md` Phase 2, verdict FAIL — fix loop):**
    - **F-2-08 (copy, RESOLVED by user confirmation):** the user confirmed restoring the spec §8 verbatim copy — `RegisterForm.jsx` title "Create Account" → "Sign Up", subtitle "Sign up to start building reports" → "Create your account to get started" (also removes the internal inconsistency: header vs submit button and app-bar CTA).
    - **F-2-09 (copy, RESOLVED):** `RegisterForm.jsx` confirmPassword validate message "Passwords do not match" → "Passwords must match" (spec §8 / REQ-114 verbatim; the RHF two-argument validate form is retained — review confirmed it as functionally equivalent).
    - **C-2-01 (spec alignment, RESOLVED):** the PublicRoute Landing exception is now written into the spec — REQ-095 (requirement + acceptance), the §12.4 table row, the §12.4 `PublicRoute` detail, and the §6 Landing bullet all state: authenticated users may view `/` (Landing); `/login`, `/register`, and any future public pages redirect to `/dashboard`.
    - **C-2-02 (spec alignment, RESOLVED):** the spec now carries the settled user values — §12.3 table row, REQ-099, and the §14 GlobalSearchDialog component detail state "centered 600px wide / 80vh tall on all larger screens (≥600px)" (full-screen below 600px / 768px landscape unchanged); spec §7 Login logo rule replaces `fontSize: 48px` with the no-fixed-pixel-icon rule (`fontSize="large"` + `color="primary"`, MUI props only).
- **Validation results:**
  - `node --check` on every backend `.js` file: PASS.
  - `npm run lint` (client): PASS, exit 0.
  - `npx vite build`: 0 errors (chunk-size warning only, pre-existing); `client/dist/` deleted after build.
  - `python scripts/verify-initial-doc.py`: exit 0, SELF-ALIGNED.
  - **Live e2e against the real backend + MongoDB Atlas (per `backend/.env` `MONGODB_URI`):** register 201 with §11.4 names (dotted `beza.ayalew@` → beza/ayalew; non-dotted `sami@` → sami/sami); duplicate 409; invalid payload 422 shape `{ success, message: 'Validation failed', data: { errors: [{ field, message }] } }`; login 200 with both cookies (`accessToken` Max-Age 900 Path=/; `refreshToken` Max-Age 604800 Path=/api/v1; both HttpOnly + SameSite=Lax, `secure` absent in dev — correct); wrong password 401; `GET /auth/me` 200 with cookies, 401 without; refresh rotation 200 → old-token replay 401, tampered cookie 401; logout 200 → me 401; `GET /oauth/google` 503 (placeholders); rate limit 429 on the 21st rapid login (20/15min tier); `GET/PUT /user/profile` 200 with cookies, 401 without.
  - Cleanup after e2e: probe users deleted from Atlas; port 4000 released; no leftover test data.
  - **Environment note:** `backend/.env` points at the Atlas cluster (`report-builder-v1` database name) — the earlier assumption of a local MongoDB was wrong; e2e ran against Atlas and the probe users were fully removed.

### Phase 3 — Domain Models And Core Reporting

- **Branch:** `phase-3-domain-models-and-core-reporting` — **Commit:** `feat: phase 3 domain models and core reporting`
- **Date:** 2026-08-03
- **Implemented:**
  - **Backend (T-3-01..T-3-04; `## API Contract` §8, `## Report Domain`, `## Status Machine`, `## Work Flow` W-01..W-04, §6/§22 versioning, AD-010):** 4 new controllers replacing the Phase 1 route scaffolds:
    - `backend/controllers/branch.controller.js` — `listBranches` (paginated, `{user, isArchived: false}` default, optional `search` name regex, sort `{name: 1}`), `getBranch`, `createBranch`, `updateBranch` (409 on archived), `deleteBranch` (plain CRUD; Phase 8 T-8-02 replaces it with the §35 two-path lifecycle).
    - `backend/controllers/report.controller.js` — `listReports` (paginated sort `{createdAt: -1}`, populate `branches.branchId` name/location + `audio` + `transcription`; `buildListQuery` = user-scoped, `isArchived: false` default with `?isArchived` override, optional `?status`, `?date` (exact DD-MM-YYYY), `?branchId` (exact `branches.branchId`), and `?search` filters — `search` matching the date string OR owned-branch name via `branches.branchId $in`), `getReport`, `createReport` (validates every `branchId` is owned + active else 404; starts in `draft`), `updateReport` (metadata fields plus generated-text edits: the previous `generated` is pushed to `generatedHistory[]` as `{provider, text}` before replacement; provider 400-required whenever an existing generated text is replaced), `deleteReport` (plain CRUD, 200 `data: null`).
    - `backend/controllers/transcription.controller.js` — `createTranscription` (`POST /api/v1/transcriptions {reportId, raw}`: only from `audio_attached` status else 409, 409 when a transcription already exists, links `Report.transcription`, status → `transcribed`), `listTranscriptions` (paginated, optional `report` filter), `getTranscription`, `updateTranscription` (`PATCH /api/v1/transcriptions/:id {reviewed}`: allowed on `transcribed`/`reviewed` reports else 409, pushes `{instruction: '', reviewed, reviewer: req.user._id}` to `history[]`, writes `latest`, advances the report → `reviewed`).
    - `backend/controllers/audio.controller.js` — metadata-first per T-3-04c (`createAudio`: originalName/mimeType/fileSize/duration; 409 on `completed` reports; pushes to `report.audio[]`, `draft` → `audio_attached`), `listAudios` (paginated, optional `report` filter), `getAudio`.
  - Route contracts replacing the Phase 1 `/validate` scaffolds: `backend/routes/{branch,report,transcription,audio}.routes.js` — every endpoint `authenticate`-guarded with id params validated (`validateBranchId`/`validateReportId`/`validateTranscriptionId`/`validateAudioId`); `backend/routes/index.js` mounts `/branches`, `/reports`, `/transcriptions`, `/audio` under `/api/v1` (API Contract §8 plural paths).
  - Validators finalized: `report.validator.js` (date `DD-MM-YYYY` regex, `branches` array min 1 with MongoId `branchId` + optional per-branch 12-hour clock times, `clockIn`/`clockOut` 12-hour `02:30 PM` pattern, `generated` string, `provider` `addis|gemini|nvidia` accepted only together with generated text), `transcription.validator.js` (reportId MongoId + `raw` on create; `reviewed` on update), `audio.validator.js` (clip metadata), `branch.validator.js` (name/location).
  - `backend/utils/constants.js` += `REPORT_STATUS_{DRAFT,AUDIO_ATTACHED,TRANSCRIBED,REVIEWED,COMPLETED}` + `PROVIDER_{ADDIS,GEMINI,NVIDIA}`; `backend/models/audio.model.js`: `filePath` `required: true` → `default: ''` (physical upload lands in Phase 4).
  - **Frontend (T-3-05; `## Report Management`, `## MUI Component Standards`, REQ-103/106, AD-016):** 8 new reusable components — `MuiPageHeader`, `MuiStatusBadge` (status → color map), `MuiSelect`, `MuiPagination`, `MuiConfirmDialog` (with `confirmLoading`), `MuiDataGrid` (named imports `DataGrid` from `@mui/x-data-grid/DataGrid` + `GridToolbar` from `@mui/x-data-grid/components`), `MuiDatePicker` (Ethiopian conversion wrapper: Gregorian dayjs anchor + Ethiopian DD-MM-YY display via `gregorianToEthiopian`; Gregorian month panel — full Ethiopian panel deferred per user decision), `MuiTimePicker` (`hh:mm A`).
  - `components/columns/reportColumns.jsx` + `branchColumns.jsx` (action column last — reports View/Delete with Edit → Phase 5 and Archive/Restore → Phase 8; branches View/Edit/Delete); domain components `components/report/CreateReportDialog.jsx` (metadata-only per user decision: date via RHF Controller + MuiDatePicker, branches via BranchSelectorDialog, clockIn/clockOut via MuiTimePicker), `BranchSelectorDialog.jsx`, `ReportFilterDialog.jsx` (date + single-branch select + Archived switch, per `## UI/UX Spec` §12.6), `components/branch/BranchFormDialog.jsx`.
  - Pages: `Reports.jsx` (filter badge 1–3, list/grid toggle, cards + MuiPagination or MuiDataGrid, create + delete via MuiConfirmDialog), `ReportDetails.jsx` (status badge, audio/transcription/generated + `generatedHistory[]` version list, delete), `Branches.jsx` (DataGrid + full CRUD dialogs).
  - Redux: new `redux/features/reportSlice.js` + `branchSlice.js` (`injectEndpoints` into the shared `api`, envelope unwrap via `transformResponse`, `tagTypes` Report/Branch, `useLazy*` exports); `store.js` imports the slices as side effects (single api middleware); `GlobalSearchDialog.jsx` wired to `useLazyListReportsQuery`/`useLazyListBranchesQuery` (grouped accordions, navigate on click) — the Phase 2 shell completed (T-3-01b); `AppShell.jsx` content area gains `p: 1`.
  - `utils/ethiopianDate.js` += `formatEthiopianDateLong` (DD-MM-YYYY API shape) + `parseEthiopianDate`; `utils/constants.js` += `REPORT_STATUS_*`/`PROVIDER_*`/`PAGINATION_*`.
- **Changes/updates/corrections:**
  - **User decisions (binding):** (1) report update may edit `generated` — the previous text is archived to `generatedHistory[]` `{provider, text}`; provider is 400-required when an existing generated text is replaced (BR-07/08 — editable after generation, AD-010); (2) `POST /api/v1/transcriptions {reportId, raw}` exposed for the Phase 3 narration → transcription chain (T-3-04a/b); the UI transcription review ships in Phase 4; (3) MuiDatePicker = conversion wrapper (Gregorian anchor value, Ethiopian DD-MM-YY display; full Ethiopian month panel deferred); (4) CreateReportDialog is metadata-only (recorder + per-branch clock times Phase 4); (5) per user instruction Phase 3 was developed WITHOUT commit/log until the user explicitly asked for this log entry; git commit/push still pending user approval.
  - **Store crash fix (`Duplicate middleware references`):** `api.injectEndpoints` mutates and returns the SAME `api` object — registering `branchApi`/`reportApi` as extra slices duplicated the middleware and threw at store creation. Feature slices are now plain side-effect imports; `store.js` registers only `api` + `assistantApi` middleware/reducers (pattern documented in code, REQ-103).
  - **MUI v9 reconciliations (React DOM-prop warnings — REQ-107/§14.1 sx-only rule):** `Stack` no longer accepts `justifyContent`/`alignItems` — verified against the installed `Stack.d.ts` (v9 Stack derives from Grid v2 and keeps only `direction`/`spacing`/`divider`/`useFlexGap`/`sx`) — moved to `sx` in `CreateReportDialog.jsx` (Grid) and `Reports.jsx` (Stack lines 136/165, card header + pagination rows); `disableEscapeKeyDown` removed from the MuiDialog usage in `CreateReportDialog.jsx` (MUI v9 dropped the prop; Escape/backdrop blocking is via the `onClose` reason — the no-op `onClose` already enforces §11 close behavior, with a code comment documenting the reconciliation); `DataGrid`/`GridToolbar`/Desktop/Mobile pickers use named imports from deep subpaths (`@mui/x-data-grid/DataGrid`, `@mui/x-data-grid/components`) — the exports map allows no default/root imports; `ListItem alignItems` verified still valid in v9 (unchanged, `ReportDetails.jsx`).
  - **Columns `.js` → `.jsx` rename:** JSX inside `.js` files fails the Vite/Babel transform — the `.jsx` extension is part of the columns convention.
  - **React Compiler eslint:** `BranchSelectorDialog.jsx` avoids setState-in-effect via the render-time `prevOpen` sync pattern; `GlobalSearchDialog.jsx` reads the search field with `useWatch` (RHF `watch` is flagged non-memoizable by `react-hooks/incompatible-library` — Phase 2 pattern reused).
  - **Active-only lists:** `buildListQuery` and `listBranches` default to `isArchived: false` so Phase 8's archive never leaks archived records into Phase 3 screens; the report update deliberately has no isArchived guard (no archive in Phase 3).
  - **Reports filter contract completed (audit finding — user-approved option A):** the §12.6 filter dialog and `Reports.jsx` sent `date` + `branchId` query params that `buildListQuery` initially ignored — the Date and Branch filters only incremented the filter badge without filtering the list. Per the source brief (`docs/initial-doc.md` §3.5.1.8: `GET /api/v1/reports?page=1&limit=10&date=&branch=&isArchived=`), `buildListQuery` now supports `?date` (exact DD-MM-YYYY match on `Report.date`) and `?branchId` (exact match on `branches.branchId`); `?search` remains for the GlobalSearchDialog (date-string or owned-branch-name match). Verified: `node --check` PASS, lint PASS, `vite build` 0 errors.
  - **Spec-alignment open item (recorded for Step 6):** `## API Contract` §8 names only `isArchived` among the report-list query params — the `date`/`branchId`/`search` filter contract is not written in the spec (only the read-only `docs/initial-doc.md` carries `date=&branch=`). No spec edits were made in Phase 3; the §8 query-param contract remains to be aligned in the review cycle or a later phase.
  - **Micro-fix:** `MuiDataGrid.jsx` JSDoc updated `client/src/components/columns/*.js` → `*.jsx` (stale after the columns `.jsx` rename).
  - **Auth refresh-race fix (user-reported kick-out, `me 401` → `refresh 401`):** root cause — `baseQueryWithReauth` fired one `POST /auth/refresh` per 401, and Phase 3 pages mount ≥2 concurrent queries (ProtectedRoute `getMe` + `listReports` + `listBranches` from the always-mounted `ReportFilterDialog`/`BranchSelectorDialog`), so an expired access token (15m) produced concurrent refreshes that raced the non-atomic rotation in `backend/controllers/auth.controller.js` (`findById` → compare → `issueTokens` save); a losing refresh's `clearAuthCookies` (or last-write-wins DB divergence) wiped/desynced both cookies → every later refresh 401 → REQ-105 logout. Timing evidence: `me` 401 in 1.051ms and `refresh` 401 in 6.292ms — both no-cookie branches (no DB lookup possible) — the pasted log was the aftermath of an earlier race. Fix: (1) `client/src/redux/features/api.js` — module-level refresh mutex (`refreshPromise`): exactly ONE shared `/auth/refresh` per 401 burst, all 401'd requests await it and retry, single logout on failure (REQ-105 flow unchanged); (2) `auth.controller.js` — atomic compare-and-swap rotation via `User.findOneAndUpdate({ _id: payload.id, refreshToken: token }, ...)` (only one concurrent refresh can ever win; consumed/replayed tokens match nothing → 401). User-approved: the mismatch 401 no longer calls `clearAuthCookies` — the response-level cookie wipe is what compounded the failure (it could land after a concurrent winner's Set-Cookie); the client logs out on refresh failure anyway (REQ-105), and REQ-087 only mandates rotation. React StrictMode confirmed NOT responsible (RTK Query dedupes identical queries per endpoint+args; the race is concurrent distinct requests) — the module-level mutex is StrictMode-safe and no double-response path remains.
  - **Standing guidelines added (user decision, logged under Phase 3):** new `## Standing Guidelines (Binding)` sections at the top of `docs/implementation-log.md` (before `## How To Use This Log`) and `docs/build-implementation.md` (after `## Role`) — (1) React StrictMode is never disabled/worked around and double responses (requests/refreshes/submits/toasts) must be impossible in any case; (2) verification uses only fast commands (`node --check`, `npm run lint`, one-shot `vite build`, `verify-initial-doc.py`, bounded single-shot live smokes in seconds) — long-running commands or automated test suites are never used to test one thing. Future guidelines of this kind are appended to these sections.
  - **Spec alignment (user-approved):** `## Redux RTK Query` §2 gained the mutex-serialization bullet — concurrent 401s share a single refresh call via the module-level mutex; double responses are impossible and React StrictMode is never disabled.
  - **Second kick-out repro — byte-exact forensics + runtime probe (user-reported, unresolved by the race fix):** trace `login 200 → me 200 → branches 200 → reports 200 → reports?date=18-03-2011 401 (3.373ms) → refresh 401 (3.391ms)`. The morgan sizes are exact JSON byte counts (all bodies < 1KB — `compression`'s default threshold skips them): **63 bytes = `{"success":false,"message":"Authentication required","data":{}}`** → `authenticate.middleware.js` `!token` branch — the ACCESS cookie was simply absent (15m `maxAge` elapsed during the >15-minute idle; expected). **57 bytes = `{"success":false,"message":"invalid signature","data":{}}`** → the refresh handler (not behind authenticate) passed the `!token` check but `jwt.verify(token, env.JWT_REFRESH_SECRET)` threw `JsonWebTokenError: invalid signature` — the refresh cookie WAS sent but signed with a secret the running server rejects. Candidates ruled out by byte count: `Refresh token missing`=62, `Invalid refresh token`=61, `jwt expired`=51, `jwt malformed`=53. This is NOT the concurrent-race signature (that 401s with `Invalid refresh token` after a real CAS DB miss) — no DB call happened in 3.4ms. **Decisive runtime probe** (throwaway tokens signed with `backend/.env`'s own secrets, bogus user id — no DB writes): `GET /auth/me` with a `.env`-signed access token → `401 User not found` (54B — signature VERIFIED, then no user) and `POST /auth/refresh` with a `.env`-signed refresh token → `401 Invalid refresh token` (61B — signature VERIFIED, CAS miss). Conclusion: the CURRENT server's secrets match `backend/.env` exactly and verification works — the trace's refresh cookie was signed by a DIFFERENT secret/instance, i.e., a stale legacy `refreshToken` cookie (older session/prototype, different secrets) shadowing the current one: the browser sends both cookie-path values (`/api/v1` first, then `/`), cookie parsing keeps the last sent, and `jwt.verify` sees the legacy value → `invalid signature`. Answers to the user's questions: the 7d refresh token DOES refresh and DOES rotate on every successful refresh (REQ-087 renews both cookies) — but the server rejected the presented token at the signature check before rotation could run; the kick is the REQ-105 logout on refresh failure.
  - **Dotenv hardening (Phase 3 corrections — the fix):** (1) `backend/config/env.js` now loads `backend/.env` itself via an absolute path anchored to the file (`fileURLToPath(new URL('../.env', import.meta.url))`) — the previous CWD-dependent `import 'dotenv/config'` in `server.js` resolved `.env` relative to the launch directory, so a backend started from another directory used a different (or no) env — a JWT secret mismatch across starts signs tokens one instance rejects, kicking every session with exactly the `invalid signature` above; `env.js` remains the sole `process.env` access point (REQ-083) and runs before every consumer (server/app import it first); `server.js` dropped the `dotenv/config` import. (2) `auth.controller.js` `setAuthCookies` now also expires any legacy `refreshToken` cookie at path `/` on every login/refresh (and `clearAuthCookies` on logout) — removes the stale shadow cookie that produced the observed trace. (3) `server.js` logs a startup **JWT secret fingerprint** (first 12 hex of SHA-256 over both secrets — never the values) so any future secret drift between processes is visible in one log line.
  - **Verification of the fix (fast commands only, per Standing Guidelines):** `node --check` on `config/env.js`, `server.js`, `auth.controller.js` PASS; CWD-independence smoke — `env.js` imported from the repo ROOT loads `backend/.env` (PORT=4000, secrets present) — PASS; runtime probe re-run against the restarted server: identical healthy results (secrets match, both signature paths verify, CAS path intact); `python scripts/verify-initial-doc.py` exit 0. Remaining manual confirmation (user, in the browser): clean backend restart from `backend/`, fresh login, DevTools → Application → Cookies → `localhost:4000` must show exactly ONE `refreshToken` (path `/api/v1`; no legacy `/`-path copy) and the startup log line `JWT secret fingerprint: …`; then manual `POST /api/v1/auth/refresh` → 200 with rotated cookies; idle 16+ minutes → navigate (no kick). No commit was made (user instruction).
  - **UI/UX corrections batch (user's 7-issue list, user-approved plan; no commit):**
    - **Icon-only actions on mobile portrait (§4/§12.6):** Reports and Branches header Create collapsed to `<IconButton color="primary" size="small">` + MuiTooltip below `(max-width:599.95px) and (orientation: portrait)` (same query as the MuiPageHeader subtitle) — labeled MuiButton elsewhere (`Reports.jsx`, `Branches.jsx`).
    - **Pagination visibility (§9.5):** the list-view MuiPagination renders only when `data.totalPages > 1` (i.e. resource count > 10 at the default limit) — no lone page chip (`Reports.jsx`).
    - **MuiEmptyState (§14 1.14, NEW `client/src/components/reusable/MuiEmptyState.jsx`):** centered icon-in-soft-circle + "No {resource} yet" headline + optional subtitle/action + `dense` variant; used by the Reports list empty view (with a Create Report action) and as MuiDataGrid's `noRowsOverlay` via the renamed `emptyResource` prop (was `emptyMessage`; default `"rows"`).
    - **Filter dialog (`ReportFilterDialog.jsx`):** centered on all viewports (`fullScreen={false}` overrides the responsive-fullscreen default); fields `size={{ xs: 12, sm: 6 }}` (stacked < 600px portrait, side by side from 600px incl. mobile landscape); Archived switch `labelPlacement="start"` (label left of the switch).
    - **MuiDialog content spacing (§5):** default `DialogContent pt: 2.5` — MUI v9 sets `paddingTop: 0` on content following a DialogTitle, so every titled dialog (filter, branch form, create report, confirm) sat flush under the divider; `contentSx` still overrides (GlobalSearchDialog `p: 0` unaffected).
    - **Branch form dialog (`BranchFormDialog.jsx`):** `fullScreen={false}` — centered on all viewports, same treatment as the filter dialog.
    - **MuiDataGrid mobile (§9.6):** toolbar slot now `showQuickFilter: false` + `sx: { flexWrap: 'wrap' }` — the MUI X v9 quick-filter field expands to 260px on focus and, on the non-wrapping toolbar row, pushed the buttons off narrow screens ("no toolbar at all"); `checkboxSelection` disabled below `sm` (selection column no longer crowds mobile widths; export falls back to the current view); height `{ xs: 320, sm: 400 }`; `noRowsOverlay` → dense MuiEmptyState.
    - **Eager branch fetch on `/reports` (user's navigation question, §13):** `GET /branches?page=1&limit=100&search=` fired on every Reports navigation because `useListBranchesQuery` in the always-mounted `ReportFilterDialog`/`BranchSelectorDialog` prefetched eagerly — both now pass `{ skip: !open }`, so navigating to Reports issues a single `GET /reports`; the RTK cache serves any subsequent dialog open.
    - **Navigation feedback (user-chosen: spinner + page loading state, §12/`## Frontend Architecture` §2):** `<Suspense fallback={<LoadingSpinner />}>` wraps `<Outlet />` in `App.jsx` — route changes show a full-page spinner while the lazy chunk loads (previously a blank gap); the Reports list view also shows a section LoadingSpinner while `isFetching && !data` (the DataGrid views already have skeleton loading).
    - **Spec alignment (user-approved, same precedent as §13.2):** `docs/specification.md` — §4 Page Header (mobile icon-only rule), §5 Filter Dialog (centered/layout/label-before-switch) + MuiDialog (content `pt` default, `fullScreen` override), §9.5 (pagination > 1 page), §9.6 (toolbar `showQuickFilter: false` + wrap, mobile checkbox, height, `emptyResource`), NEW §9.10 MuiEmptyState + §14 (1.1–1.14) catalog row, `## UI/UX Spec` §10 Reports (all of the above), `## Redux RTK Query` §3 (skip-until-open selection lists), `## Frontend Architecture` §2 (Suspense fallback in `App.jsx`).
  - **UI corrections batch v2 (user's four review points; no commit):**
    - **(1) MuiDialog keeps MUI's own content padding.** Reverted the global `DialogContent pt: 2.5` default (user: "DialogContent has its own padding by default") — `MuiDialog.jsx` is back to `{ overflowY: 'auto', ...contentSx }`. Verified in the installed `DialogContent.js`: default padding `20px 24px` (`16px 24px` small screens) with `paddingTop: 0` ONLY when content follows a DialogTitle — so the top-spacing fix belongs to the callers: `ReportFilterDialog.jsx` and `BranchFormDialog.jsx` now pass `contentSx={{ pt: 2.5 }}` (20px, symmetric with the side padding).
    - **(2) Selection box restored on mobile.** Removed the `useMediaQuery(theme.breakpoints.down('sm'))` gate — `checkboxSelection` is enabled on all viewports (`checkboxSelection ?? true`); the grid height stays `{ xs: 320, sm: 400 }` so it doesn't dominate small screens.
    - **(3) Create branch dialog fully aligned with the filter dialog.** `BranchFormDialog.jsx` gets the same `fullScreen={false}` (centered, already in place) + `contentSx={{ pt: 2.5 }}` (top spacing under the divider).
    - **(4) DataGrid toolbar root cause + fix.** In MUI X v9 the toolbar slot renders ONLY when `showToolbar` is true (`GridHeader.mjs`: `rootProps.showToolbar && <rootProps.slots.toolbar …>`; default `showToolbar: false` in `dataGridPropsDefaultValues.mjs`) — the previous `slots={{ toolbar: GridToolbar }}` + `slotProps.toolbar` never rendered, which is why there was "no toolbar at all". Also, `import { GridToolbar } from '@mui/x-data-grid/components'` resolves to the DEPRECATED v7 GridToolbar (the `export *` from `components/toolbar/index.mjs` wins over `toolbarV8` in `components/index.mjs`; all v7 toolbar parts are marked `@deprecated`). Fix: `MuiDataGrid.jsx` now sets `showToolbar={rest.showToolbar ?? true}` and drops the deprecated import entirely — the v9 default slot (`defaultGridSlotsComponents.mjs`: `toolbar: GridToolbar` from `toolbarV8/`) renders Columns toggle, Filter (with count badge), and the CSV export menu on every viewport; `slotProps.toolbar` = `showQuickFilter: false` + `sx: { flexWrap: 'wrap' }` + `csvExportOptions.getRowsToExport`. v9 reconciliation note: the v9 default toolbar has no density selector (the v7 density button has no v9 replacement) — the spec's older "columns, filter, density, CSV export" line is superseded.
    - **Spec alignment (user-approved):** `docs/specification.md` — §5 MuiDialog (default padding kept; callers pass `contentSx`) + Filter Dialog bullet (`contentSx={{ pt: 2.5 }}`), §9.6 (checkbox on all viewports; toolbar via `showToolbar` + v9 default; density reconciliation), §12.6 Filter Dialog (`contentSx` note), §14 catalog row (MuiDialog `contentSx`/`fullScreen` override, MuiDataGrid `showToolbar` + `emptyResource` + height; also `columns/*.js` → `*.jsx` stale ref fixed).
  - **UI corrections batch v3 (user's six review points; no commit):**
    - **(1) Branch-select empty state without a create button.** `BranchSelectorDialog.jsx` now renders `<MuiEmptyState resource="branches" dense minHeight="200px" />` when the fetch finishes with no branches (`!isFetching && !data?.docs?.length`) — the empty `<List>` is gone; the Apply footer stays (no-op on an empty list). No create button is ever offered in this dialog (creation lives on the Branches page, per the user's instruction "don't add a create button on it").
    - **(2) `csvExportOptions` DOM warning root cause + fix.** The v9 toolbar (`toolbarV8/GridToolbar.mjs`) destructures ONLY `showQuickFilter`, `quickFilterProps`, `csvOptions`, `printOptions`, `mainControls`, `additionalExportMenuItems` — `csvExportOptions` (the v7-era key) is unknown and fell through `...rest` onto a DOM element ("React does not recognize the `csvExportOptions` prop"). The correct v9 key is `csvOptions` (consumed → `<ExportCsv options={csvOptions} />` → `apiRef.current.exportDataAsCsv(options)`). Additionally, `getRowsToExport` is invoked with `{ apiRef }` only (`useGridCsvExport.mjs`), so the previous `({ selectedRowIds }) => …` signature silently returned `undefined` — selection-aware export never worked. `MuiDataGrid.jsx` now passes `csvOptions={{ getRowsToExport: ({ apiRef }) => { const selected = apiRef.current.getSelectedRows(); return selected.size ? [...selected.keys()] : undefined; } }}` — selected rows only when a selection exists, otherwise the current view (undefined → v9 default).
    - **(3) Dialog spacing pattern — user's own fix supersedes the v2 `contentSx` approach.** The user edited `MuiDialog.jsx` directly: `DialogContent` is now `{ maxHeight: 400, overflowY: 'auto', p: 0 }` (the "added maxHeight") and `contentSx` is retired; callers apply `sx={{ "& .MuiPaper-root": { m: 1, py: 1 } }}` (paper inset on small screens) plus inner padding (`px: 1, py: 2` grid / `p: 1` list). Reconciled in this batch: `MuiDialog.jsx` (contentSx prop removed from the destructure — dead code cleaned; JSDoc rewritten), `BranchFormDialog.jsx` (dead `contentSx={{ pt: 2.5 }}` removed; paper `m: 1, py: 1` + grid `px: 1, py: 2` added; JSDoc rewritten), `GlobalSearchDialog.jsx` (dead `contentSx={{ p: 0 }}` removed), `BranchSelectorDialog.jsx` (paper `m: 1, py: 1` + list `p: 1`), `ReportFilterDialog.jsx` (JSDoc only — the user already applied the paper sx to this dialog). The v2 log wording "callers pass `contentSx={{ pt: 2.5 }}`" is superseded by this pattern.
    - **(4) Create dialog actions aligned with the filter dialog.** `CreateReportDialog.jsx` previously rendered Cancel/Submit INSIDE the form content (a footer Grid after an inline divider) — they scrolled with the content. Per the user ("the proper is as filter dialog… ensure the content to overflow if something makes it to overflow"), the footer now lives in the MuiDialog `actions` slot (fixed row behind the actions divider, same pattern as `ReportFilterDialog`), the form is `id="create-report-form"`, and Submit is `type="submit" form="create-report-form"` (the BranchFormDialog submit pattern); the inline footer Grid + `Divider my: 2` were removed (the branches divider stays). Content scrolls independently inside the 400px cap.
    - **(5) No `forwardRef` on MuiDataGrid.** Per the user, the `forwardRef` wrapper is removed — `MuiDataGrid` is a plain function component (ref is not needed; no caller passes one — verified by grep) with `displayName` kept.
    - **(6) Mobile selection-box sizing — OPEN, resolution requested by the user.** User: "just you will log it to be resolved when I request" — no code change. Findings on record for when the user returns to it: the checkbox column is fixed at `width: 50` (`gridCheckboxSelectionColDef.mjs`), the grid root has `minWidth: 0, overflow: hidden, flex: 1` (`GridRootStyles.mjs`) inside `main` (`minWidth: 0` chain intact in `AppShell.jsx`), and every data column carries `flex` + `minWidth` — so the grid stays viewport-wide with an INTERNAL horizontal scrollbar; the "big" perception is not explained by column flex distribution and will need a live-browser observation (which elements exceed: checkbox column vs. whole grid vs. row height).
  - **`req.validated` shape bug — nested-per-location fix (user-reported, Phase 3 corrections v4; no commit):**
    - **Root cause.** `backend/validators/validation.js` stored the FLAT `matchedData(req)` (`{ name, location }`), but the Phase 3 controllers read the **spec-mandated nested shape** `req.validated.body` (`## Backend Architecture` §10.10 + `docs/initial-doc.md` §3.5.1.8: `req.validated = { body, params, query }`). `req.validated.body` was `undefined` → `TypeError` → 500 on EVERY branch/report/transcription/audio create/update (`branch.controller.js:59,79`, `report.controller.js:109,140`, `transcription.controller.js:24,98`, `audio.controller.js:25`) — never surfaced because Phase 3 ran no DB e2e (import smoke tests only). Verified in express-validator 7.3.2 `lib/matched-data.js`: `_.set(state, instance.path, value)` — flat by construction.
    - **Fix (spec-conformant, minimal).** `validation.js` now builds `req.validated = { body: matchedData(req, { locations: ['body'] }), params: matchedData(req, { locations: ['params'] }), query: matchedData(req, { locations: ['query'] }) }` (JSDoc updated); the two Phase 2 controllers that read flat `req.validated` were aligned: `auth.controller.js` register/login and `user.controller.js` updateProfile now destructure `req.validated.body`. No validator, route, or spec changes needed (the spec already mandates the nested shape).
    - **Verification (fast commands only).** `node --check` PASS on `validation.js`, `auth.controller.js`, `user.controller.js`; import smoke PASS; bounded runtime probe — `validate()` against a real express-validator chain produced `{"body":{"name":"A","location":"X"},"params":{},"query":{}}` — all ten `req.validated.body` access sites (branch 2, report 2, transcription 2, audio 1, auth 2, user 1) now resolve.
  - **Client identity convention: `_id` everywhere (user directive "always prefer to use _id"; Phase 3 corrections v5; no commit):**
    - **Root cause (user hit the DataGrid crash on creating a branch).** DM-03 toJSON/toObject transforms DELETE the `id` virtual (`branch.model.js`, `report.model.js`, and all models) — API docs carry `_id` only. The client read `branch.id`/`report.id` (always `undefined`): `Branches.jsx` rows → MUI X "row without id" crash; `Reports.jsx` rows → same crash in grid view, card `key={undefined}` + View/Delete acting on `undefined`; edit lookup `rows.find(b => b.id === id)` could never match (Edit silently opened Create); `BranchSelectorDialog` checkboxes/keys/`branchId` were `undefined` (Apply would 422 on a real submit); `ReportFilterDialog` branch select values `undefined`; `GlobalSearchDialog` navigated to `/reports/undefined/details`; `ReportDetails` audio/`branchId` keys `undefined` (populated branch has `_id`, not `id`). Phase 3 ran no DB e2e, so none of these surfaced until the user's live smoke.
    - **Convention (binding, applies to every phase from here).** The client's canonical entity identity is the Mongo **`_id`** — used for React keys, navigation URLs, mutation args, filter values, RTK tag ids, and any `branchId`/`reportId` payloads. `id` is NEVER read from API docs. The single MUI X bridge: `MuiDataGrid.jsx` gains `getRowId={rest.getRowId ?? ((row) => row._id)}` — rows are never forced into an `id` field (overridable per caller).
    - **All 24 call sites fixed (11 files):** `MuiDataGrid.jsx` (getRowId default + JSDoc), `branchColumns.jsx` ×3 + `reportColumns.jsx` ×2 (`row.id` → `row._id`), `Branches.jsx` (rows map `_id` + edit lookup), `Reports.jsx` (rows map `_id`, card key, View/Delete handlers), `GlobalSearchDialog.jsx` ×4 (navigation + keys), `BranchSelectorDialog.jsx` ×5 (keys/toggle/checked/apply — `branchId` payload now the real `_id`), `ReportFilterDialog.jsx` (select values), `BranchFormDialog.jsx` (`updateBranch({ id: branch._id })`), `ReportDetails.jsx` ×2 (`branchId?._id ?? branchId`, `clip._id`). Slices unchanged (`transformResponse` passes docs through; `invalidatesTags` `arg.id` is the client-passed `_id`, matching `providesTags` for the detail endpoints).
    - **Spec alignment (user-approved precedent):** `docs/specification.md` — §9.6 MuiDataGrid (rows keyed via `getRowId` default `_id`, overridable; rows never need an `id` field) + §13.2/§10.7 convention line (client identity = `_id` everywhere; DM-03 strips the `id` virtual; future phases must not read `id` from API docs).
  - **First Phase 3 review round — findings F-3-01..F-3-05 applied (review-log round 2; no commit; no spec.md edits — user directive "proceed. no commit. don't touch docs\specification.md"):**
    - **F-3-01 (fix).** `backend/models/audio.model.js` was the ONLY model missing the pagination plugin — `Audio.paginate` was `undefined` and `GET /api/v1/audio` (audio.controller.js:64) would throw 500. Added `audioSchema.plugin(mongoosePaginate)` before `mongoose.model('Audio', …)` (mirrors `report.model.js`). Live-verified: `GET /api/v1/audio` → 200 with `data.docs` array + `totalDocs`.
    - **F-3-02 (fix).** `ReportFilterDialog.jsx` — the filter draft (`draftDate`/`draftBranchId`/`draftArchived`) is now re-synced from the applied filters at RENDER time on every false→true `open` transition via a `prevOpen` snapshot (same pattern as `BranchSelectorDialog.jsx:49-53`). Mechanism note: the review finding prescribed an `open`-keyed `useEffect`, but the project's React Compiler eslint flags direct setState-in-effect (`react-hooks/set-state-in-effect`); the render-time `prevOpen` pattern achieves the identical result without it (the BranchFormDialog effect survives only because RHF `reset` is not flagged). The dialog is permanently mounted from `Reports.jsx:215`, so render-time sync is required regardless.
    - **F-3-03 (fix).** `MuiDatePicker.jsx` rewritten so the INPUT and the calendar both display the true Ethiopian date while the value contract stays a Gregorian dayjs anchor (consumers `Reports.jsx`/`CreateReportDialog.jsx` untouched): new internal `EthiopianDateField` (built from `useFieldTextFieldProps` → `unstable_useDateField` from `@mui/x-date-pickers/DateField` → `PickerFieldUI` inside `PickerFieldUIContextProvider` from `@mui/x-date-pickers/internals`) wired into both pickers via `slots.field`, with `slots.textField: EthiopianDateTextField` rendering the Ethiopian `displayValue` on a plain MuiTextField; the MUI X section/clear/open keys are stripped via `delete` (not destructuring — eslint's default `no-unused-vars` flags `_`-prefixed destructured names) so no DOM-prop warnings; `defaultOpenPickerIcon={CalendarIcon}` from `@mui/x-date-pickers/icons`. MUI X v9 evidence: `format` is string-only (can never express Ethiopian months 2 days 29–30 or month 13) so the field renders the hidden value from the Gregorian anchor (`DesktopDatePicker.js:54` defaults `slots.field: DateField`; `useDesktopPicker.mjs` injects the field with the dayjs `value`; the hidden input parses full typed strings via `updateValueFromValueStr`). Display verified with a node probe: Greg 2025-11-08/09 → "29-02-18"/"30-02-18" (Tikimt 29/30 — previously rolled to "02-03-18") and Greg 2026-09-06/11 → "01-13-18"/"06-13-18" (Pagume — previously December). Correction to the finding's own samples: its pairs were off by one day/year — the codebase conversions are authoritative (`gregorianToEthiopian(2025,11,9)` → `{2018,2,30}`; `ethiopianToGregorian(2018,2,30)` → 2025-11-09 round-trip).
    - **F-3-04 (fix).** `client/src/utils/ethiopianDate.js` — `parseEthiopianDate` range guard now rejects day > 30 ONLY outside month 13 (`(month === 13 && day > 6)`): "06-13-2026" parses to `{day: 6, month: 13, year: 2026}`, "07-13-2026" → null. Probe-verified.
    - **F-3-05 (no code change, by design).** The 409 archived-branch guard on `updateBranch` is a Phase 8 alignment item (`## Report Domain`/§35 two-path lifecycle lands in T-8-02, which aligns it to 403) — the finding stands as accepted; the review log notes it. The "No DB e2e was run in Phase 3" validation line below is superseded by this round's live smoke (next bullet).
    - **Verification (fast commands only + bounded live smoke).** Live backend smoke on the Atlas DB (throwaway user, all probe data deleted afterward; port released): `POST /auth/register` 201 → `POST /branches` 201 → `POST /reports {date, branches}` 201 → `POST /audio` 201 → `GET /audio` 200 (`docs` + `totalDocs` — F-3-01 live) → `GET /branches` 200 → `GET /reports` 200 → `GET /reports/:id` 200 with `status: "audio_attached"`, `date` correct, `audio[]` and `branches[].branchId` populated — the full Phase 3 chain (draft → audio_attached) works end to end. Gates re-run: `node --check` all backend files PASS; `npm run lint` (client) PASS exit 0; `npx vite build` 0 errors + `dist/` deleted; `python scripts/verify-initial-doc.py` exit 0.
  - **F-3-03 re-implementation — runtime error caught in the browser, replaced with a fully custom field (review-round v7; no commit; no spec.md edits):**
    - **The crash.** The v6 `EthiopianDateField` threw `MUI X: The sectionListRef prop has not been initialized by PickersSectionList` at mount. Source-verified root cause: `internals/hooks/useField/useField.mjs:215-221` throws on mount whenever the field's `textField` slot does not render a `PickersSectionList` — the slot is structurally required to host the section-level editing DOM (`domGetters`/`getSectionIndexFromDOMElement` hit-test against it), and its `sectionListRef` is forked into the field's own ref. A plain `MuiTextField` slot can NEVER satisfy the machinery, so no variant of "render a normal input" inside `useDateField`/`PickerFieldUI` is viable; the field's `format` string is also Gregorian-only, so typing a Pagume month or Tikimt day 29–30 was impossible by design.
    - **The fix (user-approved Option C — fully custom `slots.field`, no section machinery at all).** `MuiDatePicker.jsx` now ships `EthiopianDateField` driven by the PUBLIC picker context (`usePickerContext` from `@mui/x-date-pickers/hooks` — verified exported subpath in v9.9.0's exports map) — no `useField`, no `PickersSectionList`, no internals imports (the v6 `unstable_useDateField`/`useFieldTextFieldProps`/`PickerFieldUI`/`PickerFieldUIContextProvider` wiring and the `delete`-stripping `EthiopianDateTextField` are gone). The field renders the project's `MuiTextField` with:
      - **Display:** `context.value` (Gregorian dayjs anchor) → `gregorianToEthiopian` → `formatEthiopianDateLong` (DD-MM-YYYY — chosen so typed text round-trips through `parseEthiopianDate`, which requires a 4-digit year).
      - **Typing:** draft-while-focused (`useState`, blur discards — no `setState`-in-effect, React Compiler-safe): `parseEthiopianDate` → `ethiopianToGregorian` → `dayjs(new Date(y, m-1, d))` → `context.setValue(value, { changeImportance: "set", source: "field" })` (fires the consumer's `onChange` AND updates the picker's internal state; popup stays open while typing). **True Ethiopian input now works** — `30-02-2018` (Tikimt 30) and `06-13-2018` (Pagume 6) are typeable; `07-13-2018` is rejected (no commit); Enter is `preventDefault`ed so a valid date never submits the RHF form.
      - **Calendar open:** input click (adornment buttons excluded via `event.target.closest('button')`) and a trailing `CalendarIcon` button (`context.setOpen(true)`), disabled/hidden per `context.triggerStatus`; `context.open` guards double-open. The filter dialog's custom end adornment (`ReportFilterDialog.jsx:119` clear button) is preserved, stacked beside the open button in one Fragment.
      - **Anchor:** the native input ref is forked (`useForkRef` from `@mui/utils` — the React Compiler's `react-hooks/immutability` rule rejects a manual `.current` write) with `context.triggerRef`, the popper's `triggerElement` anchor (`usePicker.mjs` line 245; `PickerPopper.mjs` line 325 — verified).
    - **Wrapper changes:** `slots.field = EthiopianDateField`; `slotProps.field = { label, error, helperText, fullWidth: fullWidth ?? true, ...slotProps?.field }` (the field reads them as direct props); `slotProps.input.endAdornment` passes through to the field's `MuiTextField`; the dead `format` prop is dropped (nothing consumes it anymore — the field is not section-based). Consumers (`Reports.jsx`, `CreateReportDialog.jsx`, `ReportFilterDialog.jsx`) untouched; the Gregorian-anchor value contract is unchanged. The open-button aria label moved to `client/src/utils/constants.js` (`DATE_PICKER_OPEN_ARIA_LABEL` — no magic values rule).
    - **Verification (fast commands only).** `npm run lint` PASS exit 0 (after fixing the `react-hooks/immutability` flag via `useForkRef`); `npx vite build` 0 errors + `dist/` deleted; `python scripts/verify-initial-doc.py` exit 0. Node probe of the field's exact conversion path: display `gregorianToEthiopian(2025,11,9)` → "30-02-2018"; typed "30-02-2018" → `ethiopianToGregorian` → `{2025,11,9}` (round-trips to the same dayjs anchor); "06-13-2018" → `{2026,9,11}`; "07-13-2018" → null. Remaining manual confirmation (user, in the browser): Reports filter + Create Report — type `30-02-2018` in the field and confirm the calendar shows November 2025 on open; Pagume `06-13-2018` → September 2026; the filter dialog's clear button still renders beside the calendar button; no console errors on mount. No commit was made; `docs/specification.md` untouched.
  - **Review-round 3 fixes — C-3-01 and F-3-04 (review-log round 3; v8; no commit; no spec.md edits):**
    - **C-3-01 (fix, as prescribed).** `client/src/utils/ethiopianDate.js` — `parseEthiopianDate` regex extended to `^(\d{2})-(\d{2})-(\d{2}|\d{4})$`; a 2-digit year resolves deterministically as `year + 2000` (the app's date domain — the current Ethiopian century), applied BEFORE the leap test. `client/src/components/reusable/MuiDatePicker.jsx` — the display now renders `formatEthiopianDate` (DD-MM-YY) instead of `formatEthiopianDateLong`; all JSDoc references updated (no `formatEthiopianDateLong` mentions remain in the field). Typed "30-02-18" commits Gregorian 2025-11-09 and the field displays "30-02-18"; "25-02-18" ↔ "25-02-18". The API contract strings (`formatEthiopianDateLong`, DD-MM-YYYY) are unchanged — date filter, create dialog, and backend `?date` are unaffected.
    - **F-3-04 (fix, with a documented deviation from the review's prescribed formula).** The review prescribed `year % 4 === 0 ? 6 : 5`, but the code's own calendar math shows Ethiopian leap years are exactly `year % 4 === 3`: `jdnToEthiopian`'s `r = 1460` (the 1461st day of the 4-year `DAYS_PER_4_YEARS` cycle — the leap extra day) lands on `year = 4q + 3` with month 13 day 6, and `ethiopianToJdn`'s `Math.floor(year / 4)` mirrors the same cycle; the review's own round-trip method confirms it — probing the real utilities, `ethiopianToGregorian(2016, 13, 6)` → `{2024, 9, 11}` round-trips to `{2017, 1, 1}` (the exact silent-rollover defect class the finding targets), i.e. 2016 (`% 4 === 0`) is NOT a leap year in this calendar. Real-world cross-check: Ethiopian 2015 (`% 4 === 3`) was a documented leap year (13 months, Pagume 6 = Gregorian 2023-09-11) and Ethiopian 2016 (`% 4 === 0`) was common. Implemented: `const pagumeMaxDay = year % 4 === 3 ? 6 : 5;` with the guard `(month === 13 && day > pagumeMaxDay)` applied on the C-3-01-resolved year. Probe-verified with the installed utilities:
      - `06-13-2015` → accepted `{2015,13,6}` → Gregorian `{2023,9,11}` → redisplay "06-13-15" (leap Pagume 6 round-trips — the calendar conversion handles it correctly);
      - `06-13-2016` → null (2016 common — note: the review's prescribed acceptance case; the real calendar rejects it, see the deviation above);
      - `06-13-2018` → null, `07-13-18` → null (2018 common), `05-13-2018` → accepted (Pagume 5);
      - `30-02-18` → `{2018,2,30}` → `{2025,11,9}` → redisplay "30-02-18"; `30-02-2018` → same round-trip; `25-02-18` → `{2025,11,4}` round-trip; `35-02-18` → null; `04-13-2019` → accepted (2019 leap); `01-13-2020` → accepted (2020 common, day 1).
    - **Verification (fast commands only).** `npm run lint` PASS exit 0; `npx vite build` 0 errors + `dist/` deleted; `python scripts/verify-initial-doc.py` exit 0. No backend changes (no `node --check` needed). No commit was made; `docs/specification.md` untouched.
- **Validation results:**
  - `node --check` on every backend file: PASS (re-verified after the audit-filter change to `report.controller.js`).
  - Module-import smoke tests (controllers, models, validators): PASS; `routes/index.js` import only fails on the gitignored `backend/.env` startup gate (missing env vars → `config/env.js` throws by design — unrelated to Phase 3 code).
  - `npm run lint` (client): PASS, exit 0 (re-verified after the Stack/Grid sx fixes and the MuiDataGrid JSDoc micro-fix).
  - `npx vite build`: 0 errors; `client/dist/` deleted.
  - `python scripts/verify-initial-doc.py`: exit 0, VERIFY_OK (SELF-ALIGNED).
  - Second kick-out fix: `node --check` on `config/env.js`, `server.js`, `auth.controller.js` PASS; CWD-independence smoke PASS; runtime probe (pre- and post-fix) PASS; `verify-initial-doc.py` exit 0 (see the corrections bullet above for the full fix verification).
  - UI/UX corrections batch: `npm run lint` (client) PASS, exit 0; `npx vite build` 0 errors; `client/dist/` deleted; `python scripts/verify-initial-doc.py` exit 0; no backend changes (no `node --check` needed). Manual smoke pending (user, in the browser): mobile emulation — filter/branch dialogs centered, icon-only Create, DataGrid toolbar (Columns/Filter/Export) visible with the selection column present below 600px; empty state on a fresh account; pagination with > 10 reports; Reports navigation issues exactly one `GET /reports`; route changes show the full-page spinner.
  - UI corrections batch v2: `npm run lint` (client) PASS, exit 0; `npx vite build` 0 errors; `client/dist/` deleted; `python scripts/verify-initial-doc.py` exit 0; no backend changes (no `node --check` needed). Manual smoke pending (user, in the browser): toolbar (Columns/Filter/Export) visible on all viewports, selection box present on mobile, filter/branch dialogs centered with top spacing under the divider.
  - UI corrections batch v3: `npm run lint` (client) PASS, exit 0; `npx vite build` 0 errors; `client/dist/` deleted; `python scripts/verify-initial-doc.py` exit 0; no backend changes (no `node --check` needed). Manual smoke pending (user, in the browser): branch-select empty state on a fresh account; no `csvExportOptions` warning in the console; CSV export exports selected rows only when a selection exists; dialogs keep their paper inset (`m: 1, py: 1`) with content scrolling inside the 400px cap; Create Report footer fixed behind the actions divider while long content scrolls; DataGrid still renders the toolbar on every viewport. Item (6) mobile selection-box sizing is OPEN — resolution logged when the user requests it.
  - Audits: no unused imports/vars/params (`_`-prefixed unused), JSDoc on every file, no magic values (constants imports), `httpStatus` imports, no `console.log` in backend.
  - No DB e2e was run in Phase 3 (no user request; `backend/.env` points at the Atlas cluster) — the backend contract was verified via static checks and import smoke tests; live e2e is deferred to the review cycle if requested. **SUPERSEDED** by the review-round v6 live backend smoke above (register → branch → report → audio → four GETs, all 201/200 with `data.docs` + `totalDocs`, `status: audio_attached`, populations verified).

### Phase 4 — Audio Recording And STT

- **Branch:** `phase-4-audio-recording-and-stt` — **Commit:** `feat: phase 4 audio recording and stt`
- **Date:** (to be filled at the end of Phase 4)
- **Implemented:** (summary of what was built — files, features, endpoints, components — recorded in Step 6 of Phase 4)
- **Changes/updates/corrections:** (to be recorded in Step 6 of Phase 4)
- **Validation results:** (the documented validations run in Phase 4 and their outcomes)

### Phase 5 — AI Report Generation And Correction

- **Branch:** `phase-5-ai-generation-and-correction` — **Commit:** `feat: phase 5 ai generation and correction`
- **Date:** (to be filled at the end of Phase 5)
- **Implemented:** (summary of what was built — files, features, endpoints, components — recorded in Step 6 of Phase 5)
- **Changes/updates/corrections:** (to be recorded in Step 6 of Phase 5)
- **Validation results:** (the documented validations run in Phase 5 and their outcomes)

### Phase 6 — Export And Analytics

- **Branch:** `phase-6-export-and-analytics` — **Commit:** `feat: phase 6 export and analytics`
- **Date:** (to be filled at the end of Phase 6)
- **Implemented:** (summary of what was built — files, features, endpoints, components — recorded in Step 6 of Phase 6)
- **Changes/updates/corrections:** (to be recorded in Step 6 of Phase 6)
- **Validation results:** (the documented validations run in Phase 6 and their outcomes)

### Phase 7 — Mock Data And Hardening

- **Branch:** `phase-7-mock-data-and-hardening` — **Commit:** `chore: phase 7 mock data and hardening`
- **Date:** (to be filled at the end of Phase 7)
- **Implemented:** (summary of what was built — files, features, endpoints, components — recorded in Step 6 of Phase 7)
- **Changes/updates/corrections:** (to be recorded in Step 6 of Phase 7)
- **Validation results:** (the documented validations run in Phase 7 and their outcomes)

### Phase 8 — Quality Gates And Polish

- **Branch:** `phase-8-quality-gates-and-polish` — **Commit:** `chore: phase 8 quality gates and polish`
- **Date:** (to be filled at the end of Phase 8)
- **Implemented:** (summary of what was built — files, features, endpoints, components — recorded in Step 6 of Phase 8)
- **Changes/updates/corrections:** (to be recorded in Step 6 of Phase 8)
- **Validation results:** (the documented validations run in Phase 8 and their outcomes)

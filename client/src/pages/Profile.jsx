/**
 * @module pages/Profile
 */

/**
 * Profile page shell (AppShell + Page Header).
 *
 * @returns {JSX.Element} The profile page.
 */
function Profile() {
  return <h1>Profile</h1>;
}

Profile.displayName = 'Profile';

export default Profile;

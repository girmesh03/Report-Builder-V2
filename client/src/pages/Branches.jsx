/**
 * @module pages/Branches
 */

/**
 * Branches page shell (AppShell + Page Header).
 *
 * @returns {JSX.Element} The branches page.
 */
function Branches() {
  return <h1>Branches</h1>;
}

Branches.displayName = 'Branches';

export default Branches;

/**
 * @module pages/Register
 */

/**
 * Register page shell (PublicLayout).
 *
 * @returns {JSX.Element} The register page.
 */
function Register() {
  return <h1>Register</h1>;
}

Register.displayName = 'Register';

export default Register;

/**
 * @module pages/Login
 */

/**
 * Login page shell (PublicLayout).
 *
 * @returns {JSX.Element} The login page.
 */
function Login() {
  return <h1>Login</h1>;
}

Login.displayName = 'Login';

export default Login;

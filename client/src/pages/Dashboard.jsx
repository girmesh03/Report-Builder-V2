/**
 * @module pages/Dashboard
 */

/**
 * Dashboard page shell (AppShell; no Page Header).
 *
 * @returns {JSX.Element} The dashboard page.
 */
function Dashboard() {
  return <h1>Dashboard</h1>;
}

Dashboard.displayName = 'Dashboard';

export default Dashboard;
